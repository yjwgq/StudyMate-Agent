# Personal Agent OS — M1 验收手册

> 版本：v2.1（2026-09-23，验收执行后修正两处笔误并回填验收记录）
> 核对范围：`apps/api/api/v1/{auth,chat,conversations,meta}.py`、`apps/api/core/{idempotency,locks,errors}.py`、`infra/docker-compose.dev.yml`、`tests/security/conftest.py`
> 命令环境：Windows PowerShell（全部使用 `Invoke-RestMethod` / `Invoke-WebRequest`，不用 curl）

---

## 0. 开始前的准备

### 0.1 启动开发栈

```powershell
cd "D:\bisai\StudyMate Agent"
docker compose -f infra/docker-compose.dev.yml up -d --build
docker compose -f infra/docker-compose.dev.yml ps
```

期望：postgres、redis、api 三个容器 `Up (healthy)`；caddy、web `Up`；migrate 不在列表里（一次性任务，跑完即退出，属正常现象）。

### 0.2 就绪自检

```powershell
$base = "http://localhost:8080/api/v1"
Invoke-RestMethod -Uri "$base/ready"
```

期望：`{"status":"ready"}`。M1 起该接口检查 4 项：LLM 配置、JWT_SECRET、PostgreSQL、Redis。返回 503 时 `problems` 数组会告诉你缺什么。

### 0.3 三条通用约定（读完再开始）

1. **错误响应统一格式**：`{"error":{"code":"...","message":"..."},"trace_id":"..."}`。`Invoke-RestMethod` 收到 4xx 会抛异常，异常文本里直接能看到这个 JSON——以其中的 `code` 和 `message` 为判定依据。
2. **两种 409 语义不同**（都算正常行为，判定时看 message）：
   - `该会话正在处理中，请稍后再试` —— 会话锁（B6 验的是这个）
   - `相同 Idempotency-Key 的请求正在处理中` —— 幂等占位
3. **他人的会话返回 404 而非 403**：这是刻意设计（不向调用方泄露"该 id 存在但属于别人"），判定 B4 时别把 404 当错误。

---

## B1 · 数据库迁移（16 业务表 + 3 checkpoint 表）

### 操作

```powershell
docker compose -f infra/docker-compose.dev.yml logs migrate
docker compose -f infra/docker-compose.dev.yml exec postgres psql -U postgres -d agent -c "SELECT count(*) FROM pg_tables WHERE schemaname = 'public';"
```

### 期望

- migrate 日志无报错，末尾出现 `[checkpoints] checkpoints / checkpoint_writes / checkpoint_blobs ready`。
- 表数量 = **21**：16 张业务表 + 3 张 LangGraph checkpoint 表（checkpoints / checkpoint_writes / checkpoint_blobs）+ 1 张库自带的 `checkpoint_migrations`（schema 版本元数据，`langgraph-checkpoint-postgres` 的 `setup()` 自动创建）+ 1 张 alembic_version。
- （v2.0 误写为 20，漏算了 `checkpoint_migrations`，实测 21。）

---

## B2 · 迁移幂等性

### 操作

```powershell
docker compose -f infra/docker-compose.dev.yml run --rm migrate
echo $LASTEXITCODE
```

### 期望

- alembic 无新迁移可跑、checkpoint 脚本再次输出 ready，**全程无报错**。
- `$LASTEXITCODE` 为 `0`。
- 再跑一次 B1 的表数量查询，仍是 21（没有重复建表）。

---

## B3 · 注册 → 登录 → 拉会话列表

> 说明：你之前已注册过 `test1@example.com` / `test2@example.com`。本手册用全新邮箱走完整流程；重复注册旧邮箱会返回 409「该邮箱已被注册」，也属于正确行为，可顺手验证。

### 操作（在同一个 PowerShell 窗口按顺序执行）

```powershell
# 1) 注册用户 A（新邮箱）
Invoke-RestMethod -Uri "$base/auth/register" -Method Post -ContentType "application/json" -Body '{"email":"b3.u1@example.com","password":"password123","display_name":"验收用户A"}'

# 2) 登录 A，保存 token
$r1 = Invoke-RestMethod -Uri "$base/auth/login" -Method Post -ContentType "application/json" -Body '{"email":"b3.u1@example.com","password":"password123"}'
$tokA = $r1.data.access_token
$hA = @{ Authorization = "Bearer $tokA" }
$r1.data

# 3) 拉取 A 的会话列表
Invoke-RestMethod -Uri "$base/conversations" -Method Get -Headers $hA
```

### 期望

- 注册返回 `data.user`（含 id / email / display_name）。
- 登录返回 `access_token`（30 分钟有效）、`refresh_token`、`expires_in: 1800`、`user`。
- 会话列表：`data.items` 为空数组，`data.next_cursor` 为 null。

---

## B4 · 跨租户隔离（最重要的一条验收线）

### 操作（紧接 B3 的窗口）

```powershell
# 1) 注册并登录用户 B
Invoke-RestMethod -Uri "$base/auth/register" -Method Post -ContentType "application/json" -Body '{"email":"b4.u2@example.com","password":"password123","display_name":"验收用户B"}'
$r2 = Invoke-RestMethod -Uri "$base/auth/login" -Method Post -ContentType "application/json" -Body '{"email":"b4.u2@example.com","password":"password123"}'
$tokB = $r2.data.access_token
$hB = @{ Authorization = "Bearer $tokB" }

# 2) A 发一条消息（不带 conversation_id，自动创建新会话；会调一次 LLM）
$rChat = Invoke-WebRequest -Uri "$base/chat" -Method Post -Headers $hA -ContentType "application/json" -Body '{"content":"请用两句话介绍一下你自己"}'

# 3) 从 done 事件里提取新会话 id
$convId = [regex]::Match($rChat.Content, '"conversation_id"\s*:\s*"([0-9a-f-]{36})"').Groups[1].Value
$convId

# 4) B 拉自己的列表 → 应为空
Invoke-RestMethod -Uri "$base/conversations" -Method Get -Headers $hB

# 5) 关键一步：B 用 A 的会话 id 拉消息 → 期望 404
Invoke-RestMethod -Uri "$base/conversations/$convId/messages" -Method Get -Headers $hB

# 6) 对照组：A 自己拉 → 200，能看到刚才的 user / assistant 两条消息
Invoke-RestMethod -Uri "$base/conversations/$convId/messages" -Method Get -Headers $hA
```

### 期望

- 第 4 步：B 的列表为空，**看不到** A 的会话。
- 第 5 步：**404**，错误体为 `{"error":{"code":"NOT_FOUND","message":"会话不存在"},...}`。返回 404 即通过（不返回 403 是刻意设计，见 §0.3 第 3 条）。
- 第 6 步：A 能看到 2 条消息（role 分别为 user / assistant，seq 为 1 / 2）。

---

## B5 · Refresh Token 轮转 + 重放检测 + 家族连坐

> 说明：`/auth/refresh` 只需要 body 里的 `refresh_token`，**不需要** Authorization 头。轮转后旧 token 立即作废；旧 token 被再次使用 = 判定凭据泄露，**撤销整个 family 的全部 refresh token**。

### 操作（紧接 B4 的窗口）

```powershell
# 1) 第一次刷新：用 B3 登录返回的 refresh_token（轮转，成功）
$rt1 = $r1.data.refresh_token
$rRef1 = Invoke-RestMethod -Uri "$base/auth/refresh" -Method Post -ContentType "application/json" -Body ('{"refresh_token":"' + $rt1 + '"}')
$rt2 = $rRef1.data.refresh_token
"轮转成功，新 refresh_token 前 8 位: " + $rt2.Substring(0,8)

# 2) 重放旧 token → 期望 401 TOKEN_REUSE_DETECTED
Invoke-RestMethod -Uri "$base/auth/refresh" -Method Post -ContentType "application/json" -Body ('{"refresh_token":"' + $rt1 + '"}')

# 3) 家族连坐验证：重放后签发的新 token 也已被撤销 → 同样 401 TOKEN_REUSE_DETECTED
Invoke-RestMethod -Uri "$base/auth/refresh" -Method Post -ContentType "application/json" -Body ('{"refresh_token":"' + $rt2 + '"}')

# 4) A 的 refresh token 已全部失效，重新登录（后续验收要用）
$r1 = Invoke-RestMethod -Uri "$base/auth/login" -Method Post -ContentType "application/json" -Body '{"email":"b3.u1@example.com","password":"password123"}'
$tokA = $r1.data.access_token
$hA = @{ Authorization = "Bearer $tokA" }
```

### 期望

- 第 1 步：200，返回全新的 access_token + refresh_token。
- 第 2 步：401，`{"error":{"code":"TOKEN_REUSE_DETECTED","message":"检测到令牌重放，相关会话已全部失效，请重新登录"},...}`。
- 第 3 步：**同样 401 TOKEN_REUSE_DETECTED** —— 这一条才完整证明"该用户全部 refresh token 被撤销"（B5 验收标准的前半句）。
- 第 4 步：重新登录成功。

---

## B6 · 同一会话并发消息（会话锁）

> 说明：会话锁按 `conversation_id` 独占（Redis SET NX PX 300s，正常结束立即释放）。验证方式是**两个窗口真并发**：窗口 1 发起后、流还在输出的几秒内，窗口 2 往同一会话发第二条。提示词故意写长一点，给窗口 2 留出时间。

### 操作

需要 A 的一个会话 id：直接用 B4 的 `$convId`，或新发一条消息再取一个。

**窗口 1**（保持当前窗口）：

```powershell
Invoke-RestMethod -Uri "$base/chat" -Method Post -Headers $hA -ContentType "application/json" -Body '{"content":"请写一段约四百字的散文，主题是秋天的校园，要求有细节描写","conversation_id":"<粘贴convId>"}'
```

**窗口 2**（立即新开一个 PowerShell 窗口执行；token 换成窗口里 `$tokA` 的值，或先重复 B3 第 2 步登录）：

```powershell
$hA = @{ Authorization = "Bearer <粘贴A的access_token>" }
Invoke-RestMethod -Uri "http://localhost:8080/api/v1/chat" -Method Post -Headers $hA -ContentType "application/json" -Body '{"content":"再写一段关于冬天的","conversation_id":"<粘贴convId>"}'
```

### 期望

- 窗口 2：409，`{"error":{"code":"CONFLICT","message":"该会话正在处理中，请稍后再试"},...}`。服务端同时会带 `Retry-After: 5` 响应头（PowerShell 报错里看不到该头，不影响判定）。
- 若窗口 2 晚了、窗口 1 已结束：第二条会正常执行——这是锁正常释放，不算失败。换更长的提示词再并发一次即可。
- 窗口 1 正常流式返回并结束，不受影响。

---

## B7 · Idempotency-Key 幂等重放

> 说明：判定标志是响应头 `X-Idempotent-Replay: true` 与 done 事件里的 `"replayed": true`。重放**不调模型、不落库**——用消息条数前后不变来证明。同 key 的请求若在首次还在执行时到达，得到的是 409「相同 Idempotency-Key 的请求正在处理中」（你在之前的验收里已经见过）。

### 操作（回到 B6 的窗口 1）

```powershell
# 1) 首次请求（带幂等 key；若之前跑过本节，把 key 里的 001 换成 002 等）
$k = @{ Authorization = "Bearer $tokA"; "Idempotency-Key" = "b7-acceptance-001" }
$r1st = Invoke-WebRequest -Uri "$base/chat" -Method Post -Headers $k -ContentType "application/json" -Body '{"content":"用一句话解释什么是幂等性","conversation_id":"<粘贴convId>"}'
$r1st.Headers["X-Idempotent-Replay"]    # 期望：空（首次请求没有这个头）

# 2) 记录重放前的消息条数（在首次之后、重发之前——首次本身会落库 2 条）
$before = Invoke-RestMethod -Uri "$base/conversations/$convId/messages" -Method Get -Headers $hA
$before.data.items.Count

# 3) 用同一个 key 原样重发 → 期望重放
$r2nd = Invoke-WebRequest -Uri "$base/chat" -Method Post -Headers $k -ContentType "application/json" -Body '{"content":"用一句话解释什么是幂等性","conversation_id":"<粘贴convId>"}'
$r2nd.Headers["X-Idempotent-Replay"]    # 期望：true
$r2nd.Content                           # done 事件里应有 "replayed": true

# 4) 证明没有重复落库：条数与第 2 步相同
$after = Invoke-RestMethod -Uri "$base/conversations/$convId/messages" -Method Get -Headers $hA
$after.data.items.Count
```

### 期望

- 第 1 步首次请求正常生成。
- 第 3 步：`X-Idempotent-Replay` 头为 `true`；响应内容是第 1 步答案的**原文重发**（不重新生成，措辞与第 1 步完全一致）。
- 第 4 步：`$after.data.items.Count` 与第 2 步**相等**（重放新增 0 条）。首次请求本身落库 user/assistant 两条，属正常。
- 补充观察：幂等记录在 Redis 存 24 小时，期间同 key 一直返回重放结果。

---

## B8 · 安全集成测试

### 前提

开发栈已启动（测试通过 `127.0.0.1:15432` / `127.0.0.1:16379` 直连容器），`.env` 已配好 JWT_SECRET 与三条 DATABASE_URL 密码。

### 操作

```powershell
uv run pytest tests/security/ -v
```

### 期望

- 全部通过（结尾 `N passed`，无 failed / error）。
- 覆盖三组用例：跨租户越权（tenant_isolation）、refresh 重放与家族撤销（auth_replay）、并发与幂等（concurrency_idempotency）。

### 常见失败原因

- `JWT_SECRET 未配置`：conftest 在导入阶段就会报 RuntimeError，补 `.env` 后重跑。
- 连接超时：开发栈没起，或 `.env` 里三条 DATABASE_URL 的密码与 `POSTGRES_PASSWORD` 不一致。

---

## 验收记录

> 2026-09-23 执行，B1–B8 全部通过。执行中发现并修复 3 个问题（详见备注列），
> 涉及修复的用例均重跑确认。

| # | 验收项 | 结果 | 日期 | 备注 |
|---|--------|------|------|------|
| B1 | 迁移建表（21 张） | ✅ 通过 | 2026-09-23 | 实测 21 张：v2.0 误写 20，漏算 LangGraph 自带的 `checkpoint_migrations` |
| B2 | 迁移幂等 | ✅ 通过 | 2026-09-23 | 重跑 exit 0，表数不变 |
| B3 | 注册/登录/列表 | ✅ 通过 | 2026-09-23 | expires_in=1800；中文 display_name 正确入库（Git Bash 终端显示乱码为本地编码，非服务端问题） |
| B4 | 跨租户 404 隔离 | ✅ 通过 | 2026-09-23 | B 拉 A 的会话 → 404 NOT_FOUND；A 自见 2 条（seq 1/2） |
| B5 | 轮转 + 重放检测 + 家族连坐 | ✅ 通过 | 2026-09-23 | **首轮抓到真实 bug**：重放分支在事务块内 raise，家族撤销随 401 回滚失效；已改为先提交撤销再抛错（auth.py），修复后 rt2 同样 401 |
| B6 | 会话锁并发 409 | ✅ 通过 | 2026-09-23 | 真并发：窗口 2 得 409「该会话正在处理中」+ Retry-After: 5，窗口 1 流式完成（302 事件块） |
| B7 | 幂等重放不落库 | ✅ 通过 | 2026-09-23 | **首轮抓到真实 bug**：chat.py 早期占位（SET NX）与锁内正式占位自撞，带 key 的新请求必然 409；已拆分 peek（只读）/acquire（占位）。修复后重放 header=true、replayed=true、新增 0 条。v2.0 第 4 步基准笔误已随本次修正 |
| B8 | 安全测试全绿 | ✅ 通过 | 2026-09-23 | 修复 pytest-asyncio 1.x loop scope 配置（Event loop is closed）后 **11 passed**；另单元测试 12 passed；ruff / mypy 全绿 |

## 全部通过后

1. 在 `项目实施计划.md` §6 的 M1 小节逐项打勾（M1-1 到 M1-9、验收 B1–B8）。
2. 更新 `README.md`「当前进度」表：M1 → ✅ 完成。
3. 打标签：`git tag m1-done`。
4. 进入 M2（知识库入库管线）。
