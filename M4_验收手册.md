# Personal Agent OS — M4 验收手册

> 版本：v1.0（2026-09-24，随验收执行回填记录）
> 核对范围：`agent/graph/*`（state/planner/executor/react/budget/builder/nodes）、`agent/tools/*`（base/policy/registry/mcp_client/builtin）、`agent/llm.py`、`mcp_servers/*`（sandbox/todo/search）、`apps/api/api/v1/chat.py`
> 前置：栈已启动（Caddy `http://127.0.0.1:8080`）、M3 评测语料已入库、`LLM_*`/`EMBED_*` 有效

## 流程概览

```
POST /chat（认证/锁/幂等/用户消息落库 —— M1 契约不变）
  → Agent 图（LangGraph，thread_id = conversation_id，AsyncPostgresSaver）
      planner（LLM 产出 DAG：Step[] + depends_on，≤6 步；解析失败回退 chat + 降级标记）
      ├─ chat 分支 → M3 单路 RAG（检索 → citations → 流式生成 → groundedness 重写一次）
      └─ plan 分支 → execute_dag（依赖就绪的 steps 并行 gather，上限 3；重试 2 次；
      │               失败跳过传递依赖；重规划 1 次；预算 80%/100% 降级）
      │                 每 step = ReAct 循环（四类终止条件 + 循环检测）
      │                 工具经 ToolRegistry：策略(fail-closed) → 参数校验 → 风险分级
      │                 → 幂等去重 → 限流 → 超时 → 截断+full_ref → 埋点 → 审计
      └─ → synthesize（步骤结论 + 全局重编号引用 → 流式汇总 → groundedness 标注）
  → 助手消息落库（citations/degraded/usage） → done（含 plan 时间戳摘要）
```

## 验收记录（2026-09-23/24 执行，E1–E8 全部通过）

| # | 验收项 | 结果 | 证据与备注 |
|---|--------|------|-----------|
| E1 | 调研类问题 → planner 多 step，独立 step 并行执行 | ✅ | 「分别调研监督学习和强化学习并对比」→ `mode=plan`，3 步 DAG；step1/step2 起止时间戳 `[43872.26 → 43900.88]` 与 `[43872.26 → 43925.72]` **同时开始、区间重叠**。done 事件携带 plan 时间戳摘要（`plan[].started_at/finished_at`）。强断言：`tests/integration/test_agent_graph.py::test_e1`（两个 0.8s 慢工具并行墙钟 < 串行 1.6s） |
| E2 | 「加一条待办」→ todo 工具落库 | ✅ | `todos` 表出现 `title=开组会, detail=下周一早上九点开组会, dedupe_key=sha256(...)`——**dedupe_key 由治理层注入**（LLM schema 不可见，不可伪造租户/幂等键） |
| E3 | 「计算 1234 × 5678」→ sandbox 工具 | ✅ | 答案含 7006652；`tool_invocations`: `sandbox, result_ok=true, latency=705ms, args={"code":"_ = 1234 * 5678"}`。沙箱 = RestrictedPython + 孙进程（`python -I`）+ POSIX rlimit（CPU 3s/内存 256MB/禁 fork）+ 6s 墙钟杀 |
| E4 | 超长工具结果 → 截断 + full_ref | ✅ | 多主题检索 8000 字 → `tool_invocations.truncated=true, result_chars=4080`；Observation 带 `[结果过长已截断，全文引用 full_ref=…，可用 fetch_full 工具取回]`；Agent 实际调用了 `fetch_full` 取回全文 |
| E5 | 反复调用同一工具 → 循环检测终止 | ✅ | 强断言在集成测试（相同 tool+canonical args 第二次出现 → `LOOP_OBSERVATION` 回灌 → 不再执行）；真实端点回归：诱导 20 次调用 → 1 次调用 + 流正常 stop |
| E6 | tool_invocations 埋点完整 | ✅ | 12 行样例：tool_name/risk_level/result_ok/latency_ms/truncated/deduplicated 全非空；成功、失败、去重、截断路径都各有一次记录 |
| E7 | 未注册工具被拒 + 审计 | ✅ | `filesystem_delete` → 答案明确拒绝（模型自拒与 policy 拒绝双重防护；policy 层强断言在集成测试：`audit_logs` 出现 `tool.denied, target=filesystem_delete` + 埋点 `TOOL_NOT_ALLOWED`） |
| E8 | LangGraph reducer 单测 | ✅ | `tests/unit/test_state.py` 10 passed：并行写 citations 去重不丢数据（含真实 LangGraph 双 START 并行图端到端）+ channel-reducer 绑定内省断言 |

### 预算与降级的行为证据（E1 附带发现）

E1 真实运行中 search 工具（DuckDuckGo 容器网络被防爬拦截）持续失败重试，token 消耗触达 80% 阈值 → **step3（汇总）被显式跳过**（`status=skipped, error=接近预算上限，未启动`），synthesize 如实向用户说明覆盖局限（"联网检索两次失败……建议以 Sutton & Barto 教材核实"）——§7.8 的降级矩阵与 §8.3「降级必须显式」按设计工作。

## 验收过程发现并修复的缺陷

1. **`_last_user_text` 拿到空串**（高危）：LangGraph 的 `add_messages` 把输入 dict 转成 LangChain 消息对象（`type="human"` 而非 `role="user"`），取消息只认 dict → planner 对**空问题**规划，全链路假答。修复：兼容 dict 与消息对象两种形态。
2. **MCP 长会话跨任务崩溃**（高危）：stdio 会话的 anyio cancel scope 与建立它的请求任务绑定，后续请求使用/退出会触发 `cancel scope in a different task`（流随机 500）。修复：改为**按调用开短会话**（spawn→call→close 同任务内完成），代价 100–300ms/次，M7 若成瓶颈再引入 supervisor-task。
3. **MCP 双向名字丢失**：适配器把 server 的 `create_todo` 改名为本地 `todo` 后，调用也用了 `todo` → server 返回 "Unknown tool"。修复：`McpTool` 同时保存本地名（policy 对齐）与远端名（协议调用）。
4. **MCP env 白名单**：SDK 的 `env=None` 是最小默认环境**不是继承**——子进程拿不到 `DATABASE_URL_WORKER`。修复：显式传 `dict(os.environ)`。
5. **MCP isError 不检查**：fastmcp 把工具内部异常包成 `isError=true` 的结果文本，适配器当成功结果回灌。修复：`isError` → 抛错（fail-visible）。
6. **react 二次截断破坏 full_ref**（E4 实锤）：react 层对 observation 再切 `[:4000]`，恰好切掉注册表截断提示里的 `full_ref=` 后缀。修复：注册表是**唯一截断点**，移除二次切片。
7. **planner 提示词 `.format()` 冲突**：提示词含 JSON 花括号示例，`str.format` 把 `{"mode"…}` 当格式字段解析 → KeyError。修复：用 `replace` 注入 max_steps。
8. **工具注册拖垮整个注册表**：单个 MCP 工具策略缺失 → ValueError 中断构建，聊天全挂。修复：逐工具容错（未上架的跳过并告警，fail-closed 语义不变）。
9. **B7 断言演进**（有意的行为变更）：M4 起 planner+生成是多次模型调用，幂等的本质是「重放零新增副作用」而非「恰好一次调用」——断言改为 `fake.calls == calls_before_replay`。

## 已知限制（如实记录）

- **search 工具在容器网络不可用**（DuckDuckGo HTML 防爬）；生产用 `SEARCH_PROVIDER=tavily + SEARCH_API_KEY`。E1 的并行/预算/降级路径恰因此获得真实压力测试。
- **E7 的第一道防线是模型自拒**：对明显恶意的请求，现代模型通常自己拒绝（本轮未触发 policy 层）。policy 层的 fail-closed 拒绝 + 审计有自动化强断言（集成测试）。
- **checkpoint 的 LangChain 消息对象**只在会话内存续期使用，产品侧消息仍以 `messages` 表为权威（citations/degraded/usage 落库不受影响）。

## 单元/集成测试与质量门禁

- 全量 **118 passed**：unit（含 E8 reducer 10 条 / 脱敏 / 幂等重放契约）+ security（B1–B8 回归，B7 断言演进）+ **integration 7 条（E1–E7 fake LLM 全图，不烧额度，§16）**
- `ruff check .` 全过；`mypy agent apps evals worker scripts` 0 error（74 文件）
- 新增依赖：`langgraph 1.2.12`、`langgraph-checkpoint-postgres 3.1.2`、`RestrictedPython 8.5`、`mcp 1.30.0`

## 手动复验指引

```powershell
# 0) 全图集成测试（fake LLM，不烧额度，E1–E7 强断言）
uv run pytest tests/integration -v

# 1) 真实端点验收 E1–E8（消耗 LLM 额度）
uv run python scripts/m4_acceptance.py

# 2) 单测 E8
uv run pytest tests/unit/test_state.py -v
```
