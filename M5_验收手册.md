# Personal Agent OS — M5 验收手册

> 版本：v1.0（2026-09-24，随验收执行回填记录）
> 核对范围：`agent/graph/{builder,react}.py`、`agent/tools/{base,policy,registry}.py`、`mcp_servers/email_server.py`、`apps/api/api/v1/{chat,chat_runtime,approvals,chat_deps}.py`、`apps/api/repositories/approvals.py`、`apps/web/app/page.tsx`
> 前置：栈已启动（Caddy `http://127.0.0.1:8080`）、`.env` 的 `LLM_*`/`EMBED_*` 有效、`migrate` 已跑（含 checkpoint 授权）

## 流程概览

```
POST /chat（投递，立刻返回 {message_id, stream_url}）
  → asyncio.create_task(run_turn_task)：Agent 图在后台推进（**不绑定 HTTP 生命周期**）
       planner →（chat | execute_dag → ReAct）
         工具经 ToolRegistry 治理；L2（不可逆/外发）→ approvals(pending)
           → LangGraph interrupt() 暂停（checkpoint 落 Postgres）
           → SSE 发 approval 事件 + done{awaiting_approval}
  → GET /chat/{mid}/stream：Redis Stream 事件订阅（XRANGE 补历史 + XREAD 实时）
       每条 SSE 带 id → 断线/续跑用 Last-Event-ID 续读
  → POST /approvals/{id}/decide：状态机 pending→approved/rejected
       然后 Command(resume=决策) 从 checkpoint 续跑（新进程同样成立）
  → POST /chat/{mid}/cancel：Redis 标记，后台任务在下一个事件处退出（消息 cancelled）
  → POST /chat/{mid}/regenerate：新消息 + 旧消息 superseded_by
面试：会话锁（B6）保证同会话串行；幂等（B7）保证重放零副作用
```

## 验收记录（2026-09-24 执行，F1–F6 全部通过）

| # | 验收项 | 结果 | 证据与备注 |
|---|--------|------|-----------|
| F1 | 让 Agent 执行"发邮件"类动作 → 被拦截 → 前端审批 → 批准后执行 | ✅ | 审批事件含 `tool/args/risk_level=2/reason=参数触发风险升级：收件人含外部域`；批准后续跑完成，**容器内 outbox 0→1**，`approvals.status=executed`，埋点 `send_email result_ok=t` |
| F2 | 审批选"拒绝" → Agent 给替代回答，不卡死 | ✅ | 拒绝后 `approvals=rejected`、outbox 未增长；模型答："我调用了 send_email…但工具返回审批拒绝…**邮件没有发出去**"（如实转述，未编造成功） |
| F3 | 审批挂起 → **kill -9 api 容器** → 重启 → 批准 | ✅ | 中断后 `docker kill` → `up -d` → **新进程**中 decide → 从 Postgres checkpoint 续跑完成，`approvals=executed`，邮件实际发出。**中断状态完全不在进程内**（checkpoint + approvals 表） |
| F4 | 发起长任务 → 关闭浏览器标签 → 稍后重开 | ✅ | 断开订阅后轮询消息落库：`completed`，内容 2741 字符；重连 `done=stop`。前端已补会话历史加载（重开页面直接渲染完整答案） |
| F5 | 点"停止生成" | ✅ | `POST /cancel` 200 → 消息终态 `cancelled`（保留已生成内容 2 字符——长文刚开始即被停），不计入评测 |
| F6 | 点"重新生成" | ✅ | 新消息 `b6b6d1f9…` 生成完成；旧消息 `superseded_by` 指向新消息（不原地覆盖） |

### F3 的执行链路（最有说服力的一条）

```
19:59:54  send_email → APPROVAL_REQUIRED（审批落库，图 interrupt 暂停）
          docker kill api && docker compose up -d api
20:00:09  send_email → result_ok=true   ← **新进程**执行，从 checkpoint 续跑
20:00:11  list_outbox → ok
approvals: executed
```

## 验收过程发现并修复的缺陷（10 项）

| # | 缺陷 | 影响 | 修复 |
|---|------|------|------|
| 1 | **checkpointer 从未真正生效**（M4 遗留） | app_worker 对 schema public 无权限 → 初始化失败被吞 → 图**无持久化运行**，中断恢复静默失效（F3 才暴露） | `setup_checkpoints.py` 补授权；运行时 setup 失败不致命；**改用连接池**（单连接被并发轮次踩坏 → `connection is closed` → 答案空） |
| 2 | SSE 无 `id:` 行 | 审批 resume 后订阅从 0 读，立刻撞上上一轮 done → 误判"续跑没执行"（F1/F2/F3 假失败） | SSE 带 Redis entry id；前端与验收脚本用 `Last-Event-ID` 续读 |
| 3 | ApprovalInterrupt 被 registry 兜底吞掉 | `except Exception` → `TOOL_FAILED`，审批到不了人工面板 | 兜底前显式放行 ApprovalInterrupt |
| 4 | resume 重跑 → 重复创建孤儿审批 | 每次恢复留一行 pending（面板出现幽灵卡片） | `find_reusable`（同 thread+工具+参数，pending/approved 复用） |
| 5 | resume 重跑 → LLM 重新决策 → 参数漂移 | 找不到已批准审批（再次暂停死循环）；更危险：**批准 A 却执行 B** | **ReAct 决策记忆化**（Redis，按会话+步骤+轮次+消息哈希缓存首次决策，TTL 1h）→ 重放逐字节一致 |
| 6 | asyncpg UUID 直接喂 `UUID()` | regenerate / verify / mark_executed 抛 AttributeError（regenerate 500） | 边界统一 `UUID(str(...))` |
| 7 | MCP 数组参数被当字符串 | `send_email(to=[...])` 参数校验必失败，模型根本调不动工具 | 动态 schema 支持 `array → list[T]` |
| 8 | planner 提示词未覆盖"动作类"请求 | 「帮我发邮件」被判 chat 模式（chat 分支无工具）→ 模型答"环境没有该工具" | 提示词显式列出动作类触发条件（发邮件/建待办/计算/搜索/调研） |
| 9 | 后端任务初始化失败静默死亡 | 无事件、无终态、锁泄漏；订阅端只见 STREAM_GONE | 初始化纳入 try；订阅端加启动宽限期（MCP 发现耗时数秒） |
| 10 | XREAD 不支持 `(id` 独占语法 / bytes id | 续读报 `Invalid stream ID` | XRANGE 与 XREAD 分离 cursor；id 统一解码 |

### 顺带发现的产品级观察（M9 素材）

用「给外部合作伙伴发邮件」的真实措辞做 F1 时，模型**拒绝执行**并引用了一条**检索到的既有待办**（"外部域邮件需人工审批…由你手动发送"）作为依据——即**用户数据被当作指令遵循**（间接注入的真实案例）。已按 M9「记忆安全」条的思路记录（来源隔离 + 类型管控），不作为 M5 缺陷。

## 单元/集成测试与质量门禁

- 全量 **120 passed**：unit（+脱敏/状态机/reducer）+ security（B1–B8，两项已适配两步流）+ integration（**E1–E7 + F1/F2 审批预演**，全 fake LLM 不烧额度）
- `ruff check .` 全过；`mypy agent apps evals worker scripts` 0 error（79 文件）
- 前端 `npx tsc --noEmit` 0 error
- 新增依赖：`psycopg-pool`（checkpointer 连接池）

## 手动复验指引

```powershell
# 端到端验收 F1–F6（真实 LLM；F3 会 kill/重启 api 容器）
uv run python scripts/m5_acceptance.py

# 全图集成测试（fake LLM：E1–E7 + 审批批准/拒绝）
uv run pytest tests/integration -v

# 浏览器演示（建议录屏 F3/F4）
#   http://localhost:8080     发「给 partner@corp.com 发邮件约会议」→ 审批卡片 → 批准
#   审批挂起时执行：docker compose -f infra/docker-compose.dev.yml kill api
#                   docker compose -f infra/docker-compose.dev.yml up -d api
#   刷新页面 → 同一审批仍可批准 → 续跑完成
```
