# Personal Agent OS — M6 验收手册

> 版本：v1.0（2026-09-24，随验收执行回填记录）
> 核对范围：`agent/retrieval/{search,hybrid,flags}.py`、`agent/provider/rerank.py`、`agent/graph/{nodes,react,builder}.py`、`agent/tools/builtin/retrieval_tool.py`、`apps/api/api/v1/{kb,meta,conversations}.py`、`evals/compare_pipelines.py`、`apps/web/app/{page.tsx,kb/debug/page.tsx}`
> 前置：栈已启动、`.env` 的 `EMBED_*`/`LLM_*`/`RERANK_*` 有效、评测语料已入库

## 流程概览

```
query（查询侧 jieba 分词）
 → 并行双路召回（每路 top 200；ADR-2 为多租户过滤召回损失留余量）
      · 向量路：cosine (<=>) + user_id 过滤 + hnsw.iterative_scan   [超时 500ms]
      · 关键词路：tsv @@ to_tsquery + ts_rank_cd（独立连接，真并行） [超时 500ms]
 → RRF 融合（k=60，纯排名、免归一化）→ 候选 50
 → Reranker（DashScope qwen3.7-text-rerank）精排 → top 6            [超时 800ms]
 → 父块回溯（parent_id）→ 注入上下文（附 chunk_id / 各路分数）
降级矩阵：任一环节失败/超时 → 显式标记（vector/keyword/rerank/retrieval）
 → AgentState.degraded → SSE degraded 事件 → messages.degraded → UI 角标
feature flag（§14.3）：config 默认 + Redis 覆写 + /api/v1/flags（dev only）
```

## 验收记录（2026-09-24 执行）

| # | 验收项 | 结果 | 证据与备注 |
|---|--------|------|-----------|
| G1 | 两套管线同集对比（nDCG@10 与 Recall@10 均高于 M3 基线） | ⚠️ **严格判据未达成**，可用性判据达成 | 见下方专节：本集饱和（① 与 ② 均 recall@10=1.0）+ 跨文档问 rel 分级属标注约定；**可测增益在依赖故障可用性：向量路故障时 ① 0.0 → ⑤ 1.0（+1.0 recall）** |
| G2 | 模拟 rerank 故障 → 答案仍生成 + 显示"未精排" | ✅ | 开关关精排：`degraded=['rerank']`、`message="未精排（排序质量下降）"`、citations 的 `rerank_score` 全为 null、答案正常生成。**另有真实故障演练**（见下） |
| G3 | 模拟 Embedding 超时/故障 → 退化为仅关键词检索 | ✅ | `vector_count=0`、`keyword_count=28`、命中来源集合 = `{('keyword',)}`、`degraded=['vector','rerank']`（rerank 亦降级）；对话链路同样带标记，答案正常生成（45 字） |
| G4 | 记录分数 | ✅ | `evals/reports/m6_scores.json`（五管线指标 + 归因 + 观测），对比明细 `m6_compare_details.jsonl` |

### 一次真实故障演练（非计划内，价值最高）

评测进行到一半时 DashScope 返回 **403 `AllocationQuota.FreeTierOnly`（免费额度耗尽）**，
精排 100%（50/50 条 query）真实故障。系统的表现正是 §8.3 要求的：

- 每次降级都有 `WARNING 精排降级（精排返回 403…），使用 RRF 顺序` 日志；
- 用户侧**零中断**：答案照常生成（G2/G3 的用例在此状态下通过）；
- 降级标记如实出现在 `degraded` 字段与前端角标文案中（"未精排（排序质量下降）"）。

> 这是"降级必须显式"最有说服力的证据：**不是演练出来的，是撞上的**。

## G1 专节：为什么严格判据没达成，以及真实增益在哪

### 读数（2026-09-24，同一批 50 条 golden，k=10）

| 管线 | recall@10 | MRR@10 | nDCG@10 | P50 | P95 |
|------|-----------|--------|---------|-----|-----|
| ① 单路向量（M3 基线） | 1.0000 | 1.0000 | 0.9978 | 157ms | 251ms |
| ② + 混合检索（关键词 + RRF） | 1.0000 | 1.0000 | 0.9978 | 158ms | 192ms |
| ③ + Rerank | 1.0000 | 1.0000 | 0.9957 / 0.9978※ | 350ms | 420ms |
| ④ 单路向量 + **向量路故障** | **0.0000** | 0.0000 | 0.0000 | 156ms | 194ms |
| ⑤ 混合 + 向量路故障（关键词兜底） | **1.0000** | 1.0000 | 0.9957 | 299ms | 427ms |

※ 精排生效时 nDCG=0.9957；精排因额度耗尽降级时退化为 RRF 顺序 0.9978。

### 结论一：主指标在该评测集上**无提升空间**（已饱和）

① 与 ② 的 recall@10/MRR@10 均为 **1.0**——10 篇主题彼此独立的短合成语料上，
单路向量与混合检索都能把正确文档召回进 top-10。这是 M3 就记录过的局限，
本里程碑按既定决定（不扩充评测集）沿用，故 ② 相对 ① 的差值为 0。

### 结论二：精排在 3 条跨文档问上改变了 top-1，方向不一致（含一个真实修复）

| query | ② RRF 顺序 | ③ 精排顺序 | nDCG |
|-------|-----------|-----------|------|
| q046「监督学习和TCP 三次握手分别是什么」 | computer_networks → ml_basics | **ml_basics → computer_networks** | 0.8918 → **1.0**（修好）|
| q047「对比一下事务 ACID与CAP 定理」 | db_systems → distributed_systems | distributed_systems → db_systems | 1.0 → 0.8918（变差）|
| q050「单元测试、RAG的定义和区别是什么」 | software_engineering → rag_notes | rag_notes → software_engineering | 1.0 → 0.8918（变差）|

**根因不是精排不准，而是评测集的标注约定**：跨文档问的 `expected_docs` 采用
「查询里第一个术语所在文档记 rel=2、第二个记 rel=1」的约定，但查询本身对两个
对象是**同等权重**（"分别是什么/有什么区别"），精排按语义相关性排序时不可能
也不应该学到这个人为约定。q046 的修复说明精排在语义上确实有效；q047/q050 的
"变差"是与标注约定的分歧。

**这条发现本身就是 M6 的收获**：把评测集跑出"精排反而降分"的结论，定位到
标注约定而非实现缺陷 —— 这正是 M7 扩充/重标 golden 时要修的第一项
（跨文档问应记 (2,2) 对称分级，或改为对"是否召回两篇"分别计分）。

### 结论三：混合检索的**硬价值**在依赖故障时的可用性（+1.0 recall）

④ vs ⑤ 是本里程碑最干净的一组对照：两者都在**向量路不可用**的条件下运行，
唯一差别是有没有关键词路。

- 单路向量管线：**recall@10 = 0.0000**（没有任何可用召回，等于答不了）
- 混合管线：**recall@10 = 1.0000**（关键词路把 50 条全部召回）

`+1.0` 的差距不是噪音、不是饱和，而是"依赖挂了系统还能用"的定量表述——
与 G2/G3 的定性验收互为印证。

### 结论四：延迟成本如实记录

精排使检索段 P50 从 158ms 升到 350ms、P95 到 427ms，**超出 §8.2 的 300ms 检索段预算**。
实测拆解（热态）：向量路 1-3ms、关键词路 3-4ms（真并行）、**精排 176-250ms**（DashScope 网络往返）。
这是「云端精排」的固有成本，处置方式已按 §8.3 设计：超时 800ms 即降级。
后续可选优化（未做）：候选从 50 收敛到 20、或精排结果做进程内缓存。

## 验收过程发现并修复的缺陷（6 项）

| # | 缺陷 | 影响 | 修复 |
|---|------|------|------|
| 1 | **hits 通道断裂**（M4 遗留）：`retrieval_tool` 把命中写进 `error_code="HITS:{json}"`，读取端读的是 `ToolResult.data` | ReAct 分支的 `state.citations/retrieved` **恒为空**（工具调用路径下引用与降级全部丢失） | 改为 `ToolResult.data={"hits":[...]}` 并带上逐路分数；补单元/集成断言 |
| 2 | 双路召回**被同一连接串行化** | 实测两路耗时相加（930ms ≈ 471+474），"并行召回"名不副实 | 关键词路改用独立 `tenant_session`（各持连接）→ 热态两路各 1-4ms 真并行 |
| 3 | RRF **同一路内重复候选重复计分** | 重复项把分数刷高（假象） | 按 (path, chunk_id) 去重，每路只计最佳排名一次（单测覆盖） |
| 4 | 逐路分数在两路都命中时丢失（`keyword_score=None`） | 调试台逐路对比失真 | `FusedCandidate.path_scores` 记录各路原始分（M6 冒烟实锤） |
| 5 | 故障模拟开关未覆盖单路管线 | `retrieval.vector.enabled=false` 时单路管线仍能召回 → 对比表出现 `delta=0` 的**假象** | 单路管线同样尊重 `flags.vector`（这正是 ④ 的语义与 +1.0 的来源） |
| 6 | 前端角标文案写死 groundedness | 出现 `degraded:rerank` 时显示"部分结论缺少资料支撑"（错误归因） | 按 flag 映射文案 + 严重度变体；历史恢复路径补 `degraded`/`citations` 字段（否则刷新即丢角标） |

## 交付物

- **检索层**：`hybrid.py`（tsquery 构建 + RRF）、`search.py`（混合编排 + 降级矩阵）、`flags.py`（config 默认 + Redis 覆写）
- **精排**：`agent/provider/rerank.py`（DashScope 原生端点，实测响应结构 `output.results[].relevance_score`，50 候选延迟中位 185ms）
- **接口**：`/kb/search` 升级为逐路对比（每命中带 `vector_score/keyword_score/rrf_score/rerank_score/sources` + `degraded` + `diagnostics`）；`/api/v1/flags`（dev only，变更落审计）
- **前端**：降级角标按 flag 文案化（"未精排/未走向量检索/…"）+ 严重度样式；引用脚注显示精排分；`/kb/debug` 逐路对比视图（生效开关、逐路条数/耗时、来源徽标、逐路分数行）
- **评测**：`evals/compare_pipelines.py`（五管线对比 + 归因 + top1 变化观测）、`scripts/m6_acceptance.py`（G1–G4 可重复执行）、报告 `m6_compare.json` / `m6_compare_details.jsonl` / `m6_scores.json`

## 已知限制（如实记录）

1. **Rerank 依赖免费额度**：当前 DashScope 账号免费额度耗尽（403 `AllocationQuota.FreeTierOnly`）。
   精排路径会自动降级（系统可用），但**精排能力实际未生效**。需在 DashScope 控制台
   开启付费（或关闭"仅免费额度"模式）后重跑 `evals/compare_pipelines.py` 刷新读数。
2. **检索段延迟超出预算**：精排使 P95 达 427ms（预算 300ms），原因与可选优化见"结论四"。
3. **评测集饱和**：主指标无区分度，M7 扩集时须同时修正跨文档问的 rel 分级口径。

## 单元/集成测试与质量门禁

- 全量 **167 passed**：unit（新增 hybrid 17 / rerank 8 / flags 10 / **降级矩阵 12**）+ security + integration
- `ruff check .` 全过；`mypy agent apps evals worker scripts` 0 error（84 文件）
- 前端 `npx tsc --noEmit` 0 error
- 新增依赖：无（`httpx` 已在用；DashScope 精排走原生 REST）

## 手动复验指引

```powershell
# 1) 降级矩阵单测（不需要栈）
uv run pytest tests/unit/test_degradation.py -v

# 2) 五管线对比评测（G1/G4；需栈 + 精排额度）
uv run python -m evals.compare_pipelines --base-url http://127.0.0.1:8080 --k 10

# 3) 端到端验收 G1–G4
uv run python scripts/m6_acceptance.py

# 4) 浏览器演示降级角标（G2/G3）
#   ① 关精排：curl -X POST http://127.0.0.1:8080/api/v1/flags -H "Content-Type: application/json" \
#              -d '{"name":"retrieval.rerank.enabled","value":false}'
#   ② 提问 → 页面出现"⚠ 未精排（排序质量下降）"角标
#   ③ 关向量路：同上把 retrieval.vector.enabled 设为 false → 角标变"未走向量检索"
#   ④ 恢复：value 传 null
#   （逐路分数与生效开关也可在 http://localhost:8080/kb/debug 直接观察）
```
