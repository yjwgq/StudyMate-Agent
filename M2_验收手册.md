# Personal Agent OS — M2 验收手册

> 版本：v1.0（2026-09-23，随验收执行回填记录）
> 核对范围：`apps/api/api/v1/kb.py`、`apps/api/core/upload_guard.py`、`apps/api/repositories/kb.py`、`agent/retrieval/{parser,splitter,denoise}.py`、`agent/provider/embedding.py`、`worker/{celery_app,db}.py`、`worker/tasks/ingest.py`
> 前置：`.env` 的 `EMBED_*` 已配置且 key 有效（维度实测 1024，与 DDL `vector(1024)` 一致）

## 流程概览

```
上传（校验/去重/落盘/202） → Celery ingest 队列
  → PyMuPDF 解析 → 页眉页脚去噪 → 父子分块（父1024/子256/重叠10%）
  → jieba 分词 → 千问批量 embedding（批 10，断点续传） → chunks 落库 → status=ready
```

## 验收记录（2026-09-23 执行，C1–C7 全部通过）

| # | 验收项 | 结果 | 证据与备注 |
|---|--------|------|-----------|
| C1 | 中文 PDF 入库 | ✅ | 上传 202 → `processing` → `ready`；chunk_count=20，parser=pymupdf。首跑曾 failed（EMBED key 401），修复 env 后 `/reingest` 成功——**顺带验证了失败状态机与手动重跑入口** |
| C2 | 父子块与向量 | ✅ | 父 4（embedding 全 NULL）/ 子 16（embedding 全有，`vector_dims`=1024）；子块 `parent_id` 非空，回溯链完整 |
| C3 | tsv 生成列 | ✅ | `SELECT count(*) FROM chunks WHERE tsv IS NULL` = **0**；`tsv @@ to_tsquery('simple','监督 & 学习')` 命中 20 块，关键词链路可用 |
| C4 | content_hash 去重 | ✅ | 同文件二次上传 202 + `duplicated:true`，返回原 doc id，chunk_count 与文档总数不变（UNIQUE(user_id, content_hash) 兜底） |
| C5 | 重跑幂等 | ✅ | `/reingest` 后 `ready`，chunks 数量不变（20）；父块 ON CONFLICT 跳过、子块按已有 embedding 的 ord 断点续传 |
| C6 | 60MB 拦截 | ✅ | 413 `UPLOAD_REJECTED`「文件超过大小上限 50MB」（流式累计，不信任 Content-Length） |
| C7 | 改后缀名拦截 | ✅ | **首轮未通过并修复**：`.pdf` 后缀装纯文本被当 text 放行——原实现只判内容类型、未与后缀声明比对。修复后 415 + `fake_extension`；正常 `.md` 上传回归通过 |

## 修复记录

1. **C7 后缀/内容一致性**（`kb.py` + `upload_guard.py`）：新增 `kind_from_suffix`，magic 判定类型与后缀期望类型不一致 → 拒绝（`fake_extension`）。修复前「fake.pdf」会被当纯文本入库（残留数据已清理）。
2. **Dockerfile 行内注释**：`COPY ... # 注释` 的行内注释被 BuildKit 解析为复制路径导致构建失败，改为独立注释行。

## 单元测试与质量门禁

- `tests/unit/`：**35 passed**（分块器 7 / 去噪 4 / 上传校验 8 / embedding 客户端 4 / M1 存量 12）
- `ruff check .` 全过；`mypy .` 0 error（57 文件）；`tests/security` 11 passed 无回归

## 手动复验指引（PowerShell）

```powershell
# 上传（202 后轮询状态）
$h = @{ Authorization = "Bearer <token>" }
Invoke-RestMethod -Uri "http://localhost:8080/api/v1/kb/documents" -Method Post -Headers $h -Form file=@"<路径>\sample.pdf"
Invoke-RestMethod -Uri "http://localhost:8080/api/v1/kb/documents/<docId>" -Headers $h

# 查库（C2/C3）
docker compose -f infra/docker-compose.dev.yml exec postgres psql -U postgres -d agent -c "SELECT chunk_type, count(*), count(embedding) FROM chunks GROUP BY 1;"
```
