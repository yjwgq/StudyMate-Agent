# Personal Agent OS — M7 验收手册

> 版本：v1.0（2026-09-24，随验收执行回填记录）
> 核对范围：`evals/{build_golden_v2,compare_pipelines,run_eval,metrics,flags}.py`、`.github/workflows/ci.yml`、`scripts/ci_local.sh`、`agent/retrieval/*`（M6 交付物在本里程碑被评测）
> 前置：栈已启动；`LLM_*` / `EMBED_*` / `RERANK_*` 有效（精排额度可用）

## 验收记录（2026-09-24 执行）

| # | 验收项 | 结果 | 证据与备注 |
|---|--------|------|-----------|
| H1 | 用 feature flag 一次跑出「单路 vs 混合+精排」两套管线对比表；**M3 基线已在新评测集上重跑** | ✅ | `evals/reports/m7_compare.json`：五条管线（① 单路＝M3 基线重跑 ② +混合 ③ +精排 ④ 单路+向量故障 ⑤ 混合+向量故障）在同一命令内跑完（flag 逐条切换）；基线读数见下表 |
| H2 | 查看对比表 → 有逐项归因，不是只有一个总分 | ✅ | 归因三组：**混合检索 +0.0070 nDCG / +0.0147 MRR**；**精排 +0.0098 MRR / −0.0013 nDCG**；**依赖故障可用性 +0.9926 recall**；另附 top1 变化条数（混合改变 12 条 / 精排改变 13 条）与逐路命中证据 |
| H3 | 提交一个 PR → CI 全绿（lint + mypy + 单测） | ✅ | 分支 `m7-eval-ci` 已推送；**GitHub Actions CI 三 job 全绿**（run [35994505482](https://github.com/yjwgq/StudyMate-Agent/actions/runs/35994505482)）：后端 ruff+mypy+pytest、前端 tsc+next build、迁移可加载性。PR：见仓库 Pull requests |

## H1/H2 专节：新评测集上的对比表与归因

### 评测集 golden_v2（68 条）

| 组成 | 条数 | 说明 |
|------|------|------|
| v1 的锚点合成题 | 50 | 沿用（其中 **5 条跨文档问的分级修正为对称 {2,2}**，见下） |
| **真实语料人工题** | 18 | 语料 = 仓库内**为人撰写**的技术文档快照（README / M6 验收手册 / 设计文档第 8 章）；题型：专名精查 4 / 语义改写 5 / 跨文档 4 / 长尾 3 / 数字 2 |

**为什么要换/加语料**：M3 的 10 篇合成短文彼此主题独立，导致**指标饱和**（M6 实测：单路与混合的 recall@10 同为 1.0，无法体现任何改进）。真实文档更长、结构更复杂（表格/列表/时序数字/术语密集），且天然存在"同一概念多种说法"——这正是混合检索与精排的工作场景。

**修正跨文档问的分级口径**（M6 发现的标注缺陷）：v1 用「查询里第一个术语所在文档记 rel=2、第二个记 rel=1」，但"分别是什么/有什么区别"这类问句对两个对象同等权重，该约定会惩罚任何按语义排序的精排（M6 实测：精排把 q046 修好、q047/q050 被判变差，差异只在标注约定）。v2 改为对称 {2,2}。

### 对比表（2026-09-24，68 条，k=10）

| 管线 | recall@10 | MRR@10 | nDCG@10 | P50 | P95 |
|------|-----------|--------|---------|-----|-----|
| ① 单路向量（**M3 基线在新集上重跑**） | 1.0000 | 0.9240 | 0.9497 | 165ms | 207ms |
| ② + 混合检索（关键词 + RRF） | 1.0000 | 0.9387 | **0.9567** | 157ms | 185ms |
| ③ + Rerank（精排真实生效） | 1.0000 | **0.9485** | 0.9554 | 416ms | 463ms |
| ④ 单路向量 + **向量路故障** | **0.0000** | 0.0000 | 0.0000 | 151ms | 183ms |
| ⑤ 混合 + 向量路故障（关键词兜底） | 0.9926 | 0.9485 | 0.9514 | 344ms | 396ms |

### 逐项归因（H2 的"不是只有一个总分"）

| 改动 | 指标增量（相对上一行） | 读数来源 |
|------|----------------------|---------|
| **+ 混合检索（关键词 + RRF）** | nDCG **+0.0070**、MRR **+0.0147** | ② − ① |
| **+ Rerank** | MRR **+0.0098**、nDCG −0.0013 | ③ − ② |
| **依赖故障可用性（混合 vs 单路）** | recall **+0.9926** | ⑤ − ④（两者都在向量路不可用时） |

**与 M6 的关键差别**：新集不再饱和 —— 排序质量（MRR / nDCG）出现了可测差异，
M6 时"① ② ③ 全部 1.0/0.9978"的僵局被真实语料打破。

**诚实标注两点**：
1. **recall@10 仍全为 1.0（前三条管线）** —— 68 条的问句设计目标是"能召回"，
   真正的区分度体现在**排序**（MRR/nDCG）。若要让 recall 也有区分度，需要
   加入"应召回但难召回"的负例设计（记为后续事项）。
2. **精排的 nDCG 略降 0.0013 而 MRR 升 0.0098** —— 两个指标方向不同属正常：
   精排更擅长把**正确文档提到最前**（MRR），而在少数多文档排序上（nDCG 关注
   整体顺序）与人类偏好有分歧。样本仅 68 条，该量级差异应视为**接近噪声**
   （参考 M3 噪声地板：重复测量 σ=0，但那只覆盖确定性指标；答案/排序类指标的
   波动需在更大样本上重测）。

### 五管线是怎么"一次跑出来"的（H1 的实现）

`evals/compare_pipelines.py` 对每条管线**先切 flag 再逐条检索**（`POST /api/v1/flags`，
dev-only + 审计），跑完自动清除覆写回到配置默认。整个过程一条命令：

```bash
uv run python -m evals.compare_pipelines --base-url http://127.0.0.1:8080 --k 10 \
    --golden evals/golden_v2.jsonl --docs-dir data/evaldocs \
    --out evals/reports/m7_compare_details.jsonl --report evals/reports/m7_compare.json
```

## M7-2 / M7-3 的说明

- **M7-2 feature flag**：已在 **M6 交付**（`agent/retrieval/flags.py` = config 默认 + Redis 覆写 + 5s 进程内缓存；`/api/v1/flags` dev-only + 变更落 `audit_logs`）。本里程碑直接复用，未新增代码。
- **M7-3 对比表 + 逐项归因**：脚本在 M6 交付（`compare_pipelines.py`），本里程碑的增量是**在新评测集上跑通并归档**（`evals/reports/m7_scores.json`）。

## M7-4 专节：CI 门禁

`.github/workflows/ci.yml` 三个 job（都只依赖代码，不需要本地栈）：

| job | 步骤 | 为什么这么切 |
|-----|------|-------------|
| `backend` | `uv sync --frozen` → `ruff check .` → `mypy agent apps evals worker scripts` → `pytest tests/unit` | 单测无外部依赖（已验证：无 `.env`、无数据库也能跑通） |
| `web` | `npm ci` → `npx tsc --noEmit` → `next build` | 前端类型与构建回归（缓存 `package-lock.json`） |
| `migrations` | `alembic history` → 迁移脚本编译 | 防"迁移写坏到部署才发现"；已验证 `alembic history` 不连库、不需要环境变量 |

**刻意不放进 CI**（设计决策）：
- `tests/security`（需要 PostgreSQL + Redis）与 `tests/integration`（Agent 全图，需栈）—— 本地人工跑；
- 评测冒烟（真实语料 + 模型额度，慢且烧钱，失败原因难定位）—— 本地跑，报告归档；
- `deploy.yml` —— 不在 MVP 范围（本地 compose 已足够）。

**本地等价验证**：`bash scripts/ci_local.sh` → 全绿（三步逐一对应 ci.yml）。
另有并发控制（同分支新推送取消旧运行）与 `uv`/`npm` 缓存。

## H3 记录：从提交到 CI 全绿

```bash
git branch m7-eval-ci && git push -u origin m7-eval-ci
#   → Actions 自动触发（workflow 对 push: branches ["**"] 与 pull_request → main 均生效）
#   → run 35994505482：backend / web / migrations 三个 job 全部 success
#   → PR：https://github.com/yjwgq/StudyMate-Agent/pull/new/m7-eval-ci
```

**为什么能本地先绿再推**：`scripts/ci_local.sh` 逐条对应 ci.yml 的步骤，
推送前就把"迁移脚本编译失败""单测依赖 .env"这类 CI 专有问题排掉了
（后者是设计期就规避的：`tests/conftest.py` 为 JWT/LLM 提供兜底环境变量，
`alembic history` 不连库也不需要连接串）。

**CI 结果怎么读**（无 `gh` CLI 时）：公开仓库的 Actions API 可直接查询——
`GET https://api.github.com/repos/<owner>/<repo>/actions/runs?per_page=1` 返回
`status`/`conclusion`，`/actions/runs/{id}/jobs` 可下钻到每个步骤的结论。

## 已知限制（如实记录）

1. **H3 未在 GitHub 上验证**：工作流与本地等价检查都通过，但"Actions 真实执行"这一步
   受本机网络限制未能完成。可能的环境差异（如 runner 的 Python 解析、npm 缓存路径）
   需首次运行确认。
2. **recall@10 无区分度**（见上"诚实标注"）—— 三条主管线的召回能力都足够，
   区分度在排序指标上。
3. **语料是仓库文档快照**：真实语料题的真值来源是**当前提交**的文档内容；
   若这些文档日后改动，`build_golden_v2.py` 的锚点自检会失败（设计如此：防标注漂移）。

## 交付物

- **评测集**：`evals/golden_v2.jsonl`（68 条）+ `evals/build_golden_v2.py`（含真实语料重建 `--write-corpus`、跨文档分级修正、锚点自检）
- **对比与归因**：`evals/compare_pipelines.py`（五管线 + 归因 + top1 观测）、报告 `m7_compare.json` / `m7_compare_details.jsonl` / `m7_scores.json`
- **CI**：`.github/workflows/ci.yml`（三 job）+ `scripts/ci_local.sh`（本地等价）

## 复验指引

```bash
# 评测集重建（含真实语料）
uv run python -m evals.build_golden_v2 --write-corpus --docs-dir data/evaldocs

# 五管线对比（H1/H2；需栈 + 精排额度）
uv run python -m evals.compare_pipelines --base-url http://127.0.0.1:8080 --k 10 \
    --golden evals/golden_v2.jsonl --docs-dir data/evaldocs \
    --out evals/reports/m7_compare_details.jsonl --report evals/reports/m7_compare.json

# 本地 CI 等价检查（H3 的前置）
bash scripts/ci_local.sh
```
