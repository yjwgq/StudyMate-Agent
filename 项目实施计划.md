# Personal Agent OS 项目实施计划

> 版本：v1.0
> 日期：2026-09-22
> 依据：《Personal Agent OS 设计文档 v1.1》（`docs/design/PersonalAgent_设计文档_v1.1.md`）
> 状态：**待审核**
> 适用环境：Windows 11 + Docker Desktop（可用）+ Python 3.12（uv）+ Node 20

---

## 0. 这份文档怎么用

**本文档是施工期的唯一执行依据。** 设计文档 v1.1 回答"做成什么样"，本文档回答"按什么顺序、分几步、每步怎么验收"。

三条使用规则：

1. **按里程碑推进，不跨步。** M0 → M10 有严格依赖关系（§4.2 有依赖图）。跳过 M0 直接写 Agent，会把环境问题和技术问题搅在一起，排查成本翻倍。
2. **每个里程碑必须通过验收清单才算完成。** 验收项都写成"可执行、可观察"的形式（能跑的命令、能看到的页面、能断言的结果），不写"实现良好""功能正常"这类无法判定的标准。未通过就停在该里程碑修，不带着问题往下走。
3. **进度表（§6）是共同的事实来源。** 每完成一项就打勾，没打勾的就是没做——不靠记忆。

**关于编号约定**：本文档中 `§X.Y` 指设计文档 v1.1 的章节；`F##` 指《评审报告》（`docs/design/PersonalAgent_设计文档_评审报告.md`）中的问题编号。两者都不是本文档自己的编号。

**分工约定**：我负责设计与编写代码；**所有运行与验收都在你本地机器上执行**（模型调用、数据入库、评测跑分都会消耗你的 API 额度，不在我的沙箱里跑）。每完成一个里程碑，我会给你一份"本次改动清单 + 验收清单 + 预期现象"，你按清单操作并回报结果。

---

## 1. 项目概览

### 1.1 一句话定位

把现有的 StudyMate Agent（单机、单用户、Chroma 单路检索的学习问答 demo）改造为一个**多租户、可观测、带人工审批的生产级 AI 助理平台**。

### 1.2 目标架构

```
浏览器
  │  HTTPS
  ▼
Caddy（反向代理，SSE 必须 flush_interval -1）
  │
  ├──► Next.js 15 前端（聊天 / 知识库 / 审批）
  │
  └──► FastAPI 接入层 ── 鉴权 · 幂等 · 租户上下文
          │
          ├──► Agent Runtime（LangGraph）Planner(DAG) → ReAct → HITL
          │       └── MCP 工具：sandbox(自建) · todo(自建) · search(自建)
          │
          └──► Retrieval Service（混合召回 → RRF → 精排 → 引用）

  PostgreSQL 16 + pgvector   ← 业务数据 + 向量 + LangGraph checkpoint
  Redis                      ← Celery broker + 会话锁 + 幂等
  Celery Worker              ← 文档入库（独立 ingest 队列）
  Langfuse Cloud             ← 全链路 trace 与 token 计费
```

### 1.3 技术栈与版本策略

| 层 | 选型 | 版本约束 |
|----|------|---------|
| 语言 | Python | **3.12**（锁定，与现有 `pyproject.toml` 一致） |
| 包管理 | uv | 保留现有的**清华 PyPI 镜像**配置（`[[tool.uv.index]]`） |
| Web | FastAPI + Uvicorn + pydantic v2 + pydantic-settings | `fastapi>=0.115` |
| Agent | langgraph + langgraph-checkpoint-postgres + mcp SDK | `langgraph>=0.3` |
| 数据库 | PostgreSQL 16 + pgvector（镜像 `pgvector/pgvector:pg16`） | pgvector **≥ 0.8**（需要 `hnsw.iterative_scan`） |
| 缓存/队列 | Redis 7、Celery 5.4+ | — |
| 前端 | Next.js 15 + React 19 + TypeScript + Tailwind + shadcn/ui | Node 20 LTS |
| 反代 | Caddy 2 | 自动 HTTPS，SSE 配置简单 |
| 模型 | LLM = **DeepSeek**；Embedding = **通义千问**（DashScope 兼容模式） | 见下方说明 |
| 观测 | Langfuse Cloud（免费层） | — |

**关于版本号的三条说明**：

1. **不写死小版本号。** 本文档只给下限约束，实际版本以 `uv add` / `npm install` 解析结果为准，并锁定在 `uv.lock` 与 `package-lock.json`。写死小版本号在一年后大概率无法安装。
2. **模型名以你控制台当前可用的为准。** 模型命名会随服务商版本迭代变化，请不要照抄本文档的示例名（如 `deepseek-chat`、`text-embedding-v3`），而是**登录控制台确认当前可用模型名**，再填入 `.env`。
3. **Embedding 维度必须与 DDL 一致。** `chunks.embedding` 定义为 `vector(1024)`。选定 Embedding 模型后，用一次真实调用确认返回维度确实是 1024；若不是，需要在 M1 建表前调整 DDL 中的维度数字，**一旦入库就不方便改了**。

### 1.4 与设计文档 v1.1 的关系

| | 设计文档 v1.1 | 本文档 |
|---|---|---|
| 回答的问题 | 做成什么样、为什么这么选 | 按什么顺序做、每步怎么验收 |
| 结构 | 架构 / ADR / DDL / 协议 | 环境 / 里程碑 / 任务分解 / 验收清单 |
| 变更频率 | 低（架构级决策） | 高（每完成一项就更新进度） |

**本文档对 v1.1 的两处补充**（v1.1 的缺口，在此补齐）：

- **代码目录结构**：v1.1 用"非功能性需求"替换了 v1.0 的代码结构章，导致新结构没有权威定义。本文档 §3.2 给出定稿结构。
- **全程云端模型（已确认，2026-09-22）**：不部署本地模型——不引入 Ollama / vLLM，不装本地 Embedding 服务。LLM 用 **DeepSeek**、Embedding 用**通义千问**、Reranker 用 `bge-reranker-v2-m3`（云端 API）。理由：本地小模型会掩盖检索层的问题、破坏评测归因，且与"应用工程"的定位无关。设计文档 ADR-8 已同步改写。差异记录见 §9。

---

## 2. 环境准备

### 2.1 前置软件清单

| 软件 | 要求 | 验证命令 | 期望输出 |
|------|------|---------|---------|
| Docker Desktop | 已启用 WSL2 后端 | `docker version` | 能看到 Client 与 Server 两段 |
| Docker Compose | v2 | `docker compose version` | `v2.x` 以上 |
| Python | 3.12.x | `python --version` | `Python 3.12.x` |
| uv | 最新 | `uv --version` | 有版本号 |
| Node.js | 20 LTS | `node --version` | `v20.x` |
| Git | 任意 | `git --version` | — |

**Docker Desktop 必须分配足够资源**：`Settings → Resources` 建议至少 **4 CPU / 8 GB 内存**。本项目同时跑 PostgreSQL + Redis + Caddy + API + Worker + Beat + Next.js，低于 8GB 会在构建镜像时卡死。

### 2.2 GPU 与模型部署方式

**本项目不需要 GPU，也不需要在本机安装任何模型。**

全程使用云端 DeepSeek（LLM）、通义千问（Embedding）与云端 Reranker。PostgreSQL、Redis、各类服务进程都是纯 CPU 负载。本机的角色只有一个：跑容器与开发环境。

也就是说，**不需要 `ollama pull`，不需要下载模型权重，不需要配 CUDA**。v1 的本地 Ollama 依赖不再使用（v1.1 设计文档 §1.3 已明确"不做模型微调"，ADR-8 已改为统一走云端）。

**唯一需要留意的是网络**：模型调用全部走公网，见 §2.3 的镜像源与 §2.4 的连通性验证。

### 2.3 网络与镜像源

中国大陆网络下，三处必须走镜像源，否则会卡在拉取阶段：

1. **Python 包**：保留现有 `pyproject.toml` 里的清华源配置（已在仓库中，不要删）。
2. **npm 包**：设置 `npm config set registry https://registry.npmmirror.com`。
3. **Docker 镜像**：在 Docker Desktop 的 `Settings → Docker Engine` 里配置 registry mirror，否则 `postgres` / `node` 等官方镜像可能拉不动。

### 2.4 首次环境验证（M0 前置）

在真正写代码前，先单独验证三件事，任何一件不通都会卡住整个 M0：

```powershell
# 1. Docker 能否跑起来一个容器
docker run --rm hello-world

# 2. pgvector 镜像能否拉到（这是后面所有数据的地基）
docker pull pgvector/pgvector:pg16

# 3. DeepSeek API 是否连通（用你控制台拿到的 key 和模型名替换）
curl https://api.deepseek.com/v1/chat/completions `
  -H "Content-Type: application/json" `
  -H "Authorization: Bearer <你的KEY>" `
  -d "{\"model\":\"<你的模型名>\",\"messages\":[{\"role\":\"user\",\"content\":\"ping\"}]}"
```

**这一步不做，M0 一旦失败就分不清是代码问题还是网络问题。**

---

## 3. 仓库重构与目录结构

### 3.1 v1 内容的清理方案（M0 的第一步）

**当前仓库清点结果**：

| 路径 | 内容 | 处置 |
|------|------|------|
| `api/`（4 个 .py） | v1 FastAPI 后端 | **删除** |
| `agents/`（5 个 .py） | v1 四大 Agent | **删除** |
| `graph/`（4 个 .py） | v1 LangGraph 工作流 | **删除** |
| `tools/`（2 个 .py） | v1 Chroma 检索 + 代码沙箱 | **删除** |
| `data_process/`（4 个 .py） | v1 数据处理与批量入库 | **删除** |
| `frontend/`（8 个 .py） | v1 Streamlit 前端 | **删除** |
| `env_check.py` | v1 环境自检（全 Ollama/Chroma 相关） | **删除** |
| `README_PHASE6.md` | v1 交付说明 | **删除** |
| `CLOUD_MODEL_GUIDE.md` | v1 云端接入指南（含已过时的"双模式"说明） | **删除** |
| `.trae/`（2 个 .md） | v1 硬性规则（已废止） | **删除** |
| `datasets/` | gsm8k/sciq/mmlu 清洗集 + 1 篇文档 + 1 个上传样例 | **删除** |
| `chroma_db/` | v1 向量库（`chroma.sqlite3`） | **删除** |
| `logs/` | v1 日志（`agents.log`） | **删除** |
| `uv.lock` | v1 依赖锁 | **删除**（与重写后的 `pyproject.toml` 冲突） |
| `.venv/` | v1 虚拟环境（v1 依赖） | **删除** |
| `.uv_cache/` | uv 包缓存 | **删除** |
| `.idea/` | IDE 配置 | **删除** |
| `pyproject.toml` | v1 依赖清单 | **保留但重写** |
| `.env.example` | v1 配置模板 | **保留但重写** |
| `.gitignore` | v1 忽略规则 | **保留但重写**（为 M10 的 GitHub 仓库预留） |
| `.env` | ⚠️ 可能含你的真实 API Key | **删除**——但**删前必须先抄下 Key 值** |
| `docs/design/` | v1.0 设计文档 / v1.1 / 评审报告 | **保留** |
| `项目实施计划.md` | 本文档 | **保留** |

**决策（2026-09-22 确认）：v2 不需要的 v1 内容一律删除，不做归档留样。**

**执行状态：已完成（2026-09-22，手动执行）**。实际删除 17 项（上表全部 + `.env`）。**唯一漏项：`.idea/` 仍在**，影响可忽略。

**⚠️ 更正记录 — 关于版本控制**

本文档此前声称"本仓库不在版本控制下（根目录无 `.git`）"，**该判断有误**。实际核查结果：仓库存在 git——提交 `dae6a9ee`，作者 `yjwgq`，创建于 2026-08-10，提交信息"初始化 StudyMate Agent 多Agent智能学习助手仓库"，且已推送到 `origin/main`（本地与远程指向同一提交）。

误判来源：早期用一次 glob 模式查询 `.git` 未命中，而该模式对根目录隐藏文件不可靠（同一模式连当时确实存在的 `.gitignore` 也未匹配到）。**把一次失败的查询当成了否定证据，未做交叉验证。**

**实际影响**：删除尚未提交，v1 文件仍完整保存在提交 `dae6a9ee` 中，随时可通过 `git restore .` 恢复——**比原先描述的"永久删除"要好**。是否保留该历史由你决定，本文档不再以"无 git"为前提。

**下一步**：按 §3.2 建立新目录骨架（已随 M0-1 交付）。

**连带影响（文档层面，已处理）**：`README_PHASE6.md` 与 `CLOUD_MODEL_GUIDE.md` 被删除后，评审报告与设计文档 v1.1 中对它们的内容引用将失去原始出处。已在两份文档中加注说明——**引用仅作为评审当时的取证记录，原文件已删除**。

### 3.2 目标目录结构（定稿）

```
StudyMate Agent/
├── apps/
│   ├── api/                        # FastAPI 接入层
│   │   ├── main.py
│   │   ├── core/                   # config / db / security / deps / errors / idempotency / locks / upload_guard
│   │   ├── api/v1/                 # auth / chat / conversations / kb / meta
│   │   └── repositories/           # auth / conversations / kb
│   └── web/                        # Next.js 15
│       ├── app/                    # App Router: chat / kb / login / register
│       └── lib/
├── agent/
│   ├── provider/                   # LLM / Embedding 客户端（两个独立开关）
│   ├── graph/                      # builder / state / planner / executor / react / nodes / budget
│   ├── tools/                      # base / registry / policy / mcp_client / builtin
│   └── retrieval/                  # parser / splitter / denoise / search / context
├── mcp_servers/                    # sandbox_server / todo_server / search_server
├── worker/                         # celery_app / db / tasks/ingest
├── infra/
│   ├── docker-compose.dev.yml      # 开发编排（caddy/api/web/worker/postgres/redis/migrate）
│   ├── docker/                     # Dockerfile.api / Dockerfile.web
│   ├── caddy/Caddyfile             # SSE 关键配置在此
│   ├── postgres/init/              # 三角色初始化（ADR-9）
│   └── alembic/                    # 迁移脚本
├── evals/                          # golden_v1.jsonl / baseline / metrics.py / noise_floor.py / run_eval.py / reports
├── scripts/
├── tests/                          # unit / integration / security
├── docs/
│   ├── design/                     # 设计文档 + 评审报告
│   └── archive/v1-rules/           # 废止的 v1 规则
├── .github/workflows/ci.yml
├── pyproject.toml                  # 保留清华源
├── .env.example
├── .gitignore
├── README.md
└── 项目实施计划.md                  ← 本文档
```

### 3.3 环境变量模板（`.env.example` 骨架）

M0 只需要前两组；其余在对应里程碑再补。

```ini
# ===== M0：最小闭环必需（推理服务，见 ADR-8）=====
# LLM：改这里无副作用，随时可换
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_API_KEY=
LLM_MODEL=                      # 填你在控制台确认的模型名

# ===== M1：基础设施 =====
DATABASE_URL=postgresql+asyncpg://app_api:CHANGE_ME@postgres:5432/agent
DATABASE_URL_WORKER=postgresql+asyncpg://app_worker:CHANGE_ME@postgres:5432/agent
DATABASE_URL_MIGRATE=postgresql+asyncpg://app_owner:CHANGE_ME@postgres:5432/agent
REDIS_URL=redis://redis:6379/0
JWT_SECRET=                     # 用 openssl rand -hex 32 生成
ACCESS_TTL_MIN=30
REFRESH_TTL_DAYS=7

# ===== M2–M3：检索 =====
# Embedding：改这里 = 全量重嵌入，启动自检会直接拦截（ADR-8）
EMBED_BASE_URL=                 # DashScope 兼容模式地址
EMBED_API_KEY=
EMBED_MODEL=                    # 填控制台确认的模型名
EMBED_DIM=1024                  # 必须与 DDL 一致

# ===== M6：精排 =====
RERANK_BASE_URL=
RERANK_API_KEY=
RERANK_MODEL=bge-reranker-v2-m3

# ===== M3：观测 =====
LANGFUSE_HOST=https://cloud.langfuse.com
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_REDACT_PII=true
```

**`.env` 绝不入库**：`.gitignore` 必须包含 `.env`，只提交 `.env.example`。

---

## 4. 里程碑总览

### 4.1 一览表

| 里程碑 | 名称 | 预估 | 核心产出 | 对应 v1.1 章节 |
|--------|------|------|---------|---------------|
| **M0** | 端到端最小闭环 | 3–5 天 | 浏览器 → FastAPI → DeepSeek → SSE 逐字回显 | §11.3（仅 token/done/error 子集，完整协议在 M5） |
| **M1** | 基础设施与多租户认证 | 1.5 周 | compose 全量编排、16 张业务表迁移、JWT + RLS、会话锁 + 幂等 | §6, §7.4, §7.6, ADR-9 |
| **M2** | 知识库入库管线 | 1.5 周 | 上传 → 解析 → 父子分块 → 千问向量化 → 异步入库 | §8.1, §13.4 |
| **M3** | 单路 RAG + 可观测 + 评测基建 | 1.5 周 | pgvector 单路检索问答带引用、Langfuse trace、baseline 与噪声地板 | §8.2, §14.1, §14.2.3 |
| **M4** | Agent 运行时 | 2 周 | DAG Planner + 并行执行 + ReAct + 工具治理 + Checkpointer | §7.1–§7.5, §7.8, ADR-4 |
| **M5** | HITL 审批 + 流式健壮性 | 1 周 | 三级风险 + 审批状态机 + 执行传输解耦 + 取消/重生成 | §7.3, §7.7, §11.3 |
| **M6** | 生产级 RAG + 降级矩阵 | 1 周 | 混合检索 + RRF + 精排 + 降级矩阵 | §8.2, §8.3 |
| **M7** | 评测对比与 CI | 0.5–1 周 | 真实语料题 + 对比表与逐项归因 + 精简 CI | §14.2, §14.3 |
| **M10** | 收尾与交付资产 | 0.5–1 周 | README/ADR/架构图 + demo 视频 + 部署验证 | §17, §20 |

**总计约 10.5 周**（M0 约 1 周 + 其余 9.5 周，含环境准备、每步验收与排障时间）。**估时宁可长而准，不要短而虚**，这本身也是要展示给面试官的能力。

### 4.2 依赖关系

```
默认按 M0 → M1 → M2 → M3 → M4 → M5 → M6 → M7 → M10 顺序推进。

硬依赖（不可颠倒）：
  M0 → M1   无数据库则认证/租户无处落地
  M1 → M2   入库需要 documents/chunks 表与租户隔离
  M2 → M3   没有数据就检索不出东西
  M3 → M4   工具层的"检索工具"依赖 M3；baseline 也是 M4 之后所有优化的对照物
  M4 → M5   审批是工具调用的一种中断形态，必须已有工具循环
  M5 → M6   降级矩阵要挂在已有的请求链路上
  M6 → M7   评测的对象是 M6 之后的完整检索链路
  M7 → M10  收尾的对比表与简历数字依赖评测结论

前端页面随各里程碑增量交付，不单独排期。
```

### 4.3 每个里程碑的统一完成流程

```
1. 我交付：代码 + 改动清单 + 验收清单 + 预期现象
2. 你执行：本地跑起来 + 逐项验收 + 回报结果（截图/日志/报错原文）
3. 通过  → 在 §6 进度表打勾 + 记下完成日期 → 进入下一里程碑
   不通过 → 我定位修复 → 重跑验收
4. 每个里程碑结束时更新 README 的进度段落（防烂尾，也是简历素材）
```

> **关于 §6 里的 `tag` 标记**：本仓库已有 git（单次提交，已推送 `origin/main`），因此各里程碑结束时可以直接打真实 tag，例如 `git tag m0-done`。

---

## 5. 里程碑详解

### M0 · 端到端最小闭环（3–5 天）

**目标**：证明"前端 → 后端 → DeepSeek → 流式返回 → 前端渲染"这条链路是通的。

**为什么放在最前**：这条链路上有三类最容易卡住的问题——Docker 网络、API Key/模型名、SSE 缓冲。**先花 3 天把它们排除掉**，后面写 Agent 和检索时就不会把"环境问题"误判成"代码问题"。这是"先端到端最小闭环"策略的全部意义。

**明确不做**：数据库、认证、RAG、Agent、多租户、Celery。**M0 的代码后面会被大幅重写，这是刻意的——它的使命是探路，不是成型。**

**任务分解**

| 编号 | 任务 | 产出 |
|------|------|------|
| M0-1 | 按 §3.1 清单删除 v1 内容（含 `datasets/`、`.venv/`、`.uv_cache/`），新建 §3.2 的目录骨架，重写 `pyproject.toml`（保留清华源）/ `.env.example` / `.gitignore` | 干净的根目录 |
| M0-2 | `infra/docker-compose.dev.yml`：只起 `api` + `web` + `caddy` | 一份能 `up` 的编排 |
| M0-3 | `apps/api`：`config.py`（pydantic-settings 读 `.env`）+ `/health` + `/ready` + `POST /api/v1/chat`（SSE 直连 DeepSeek） | 能流式返回的后端 |
| M0-4 | `apps/api/sse.py`：事件协议骨架，M0 只实现 `token` / `done` / `error` 三种事件 | 后续所有里程碑复用 |
| M0-5 | `apps/web`：Next.js 15 骨架 + 一个聊天页，用原生 `fetch` + `ReadableStream` 读 SSE 并逐字渲染 | 能看到输出的页面 |
| M0-6 | `infra/caddy/Caddyfile`：反代到 api 与 web，**聊天路由必须配 `flush_interval -1`** | 流式不被缓冲 |

**验收清单**

| # | 操作 | 期望结果 |
|---|------|---------|
| A1 | `docker compose -f infra/docker-compose.dev.yml up --build` | 三个容器都 healthy，无报错退出 |
| A2 | 浏览器打开 `http://localhost:8080`，输入"用一句话解释什么是 RAG" | 文字**逐个字出现**，不是等几秒后一次性出现 |
| A3 | `curl http://localhost:8080/api/v1/ready` | 返回 200。**注意**：M0 阶段 `/ready` 只检查 LLM 连通性（此时还没有数据库）；M1 起才加上 DB/Redis 检查（对应 §11.1 的完整语义） |
| A4 | 把 `.env` 里的 API Key 改错，重启 api，再发问 | 页面显示明确的 `LLM_OFFLINE` 错误提示，**不是白屏也不是 500** |
| A5 | 停掉 api 容器，在页面发消息 | 页面显示 `BACKEND_OFFLINE` 提示 |
| A6 | 查看 api 容器日志 | 能看到本次请求的耗时与 token 计数 |

**A2 是本里程碑的关键验收项。** 如果文字是一次性出现的，说明 Caddy（或中间的某层）在缓冲——这个问题必须在这里解决，留到后面会以为是前端 bug。

**常见坑**

- **SSE 一次性输出**：Caddy 的 `flush_interval` 没设成 `-1`。
- **容器内连不上 DeepSeek**：Docker Desktop 的 DNS 问题，检查 `Settings → Resources → Network`。
- **前端跨域**：M0 阶段 Caddy 统一入口（`/api/*` 走 api，其余走 web）可以规避；若直连 `localhost:8000` 则需要 FastAPI 配 CORS。
- **中文乱码**：Windows 终端需 `chcp 65001`。

**完成标志**：录一段 30 秒屏幕录制——输入问题、逐字输出、报错可读。这段录屏后面会剪进 demo 视频。

---

### M1 · 基础设施与多租户认证（1.5 周）

**目标**：把"能对话的 demo"变成"有数据、有用户、有隔离的后端"。

**任务分解**

| 编号 | 任务 | 关键点 |
|------|------|--------|
| M1-1 | compose 补齐 `postgres`(pgvector/pgvector:pg16) + `redis:7`，`caddy` 已就位 | 数据卷持久化 |
| M1-2 | 三个数据库角色：`app_owner`（迁移）/ `app_api`（受 RLS）/ `app_worker`（BYPASSRLS） | ADR-9 |
| M1-3 | Alembic 初始化 + 编写 v1.1 §6 的**全部 16 张业务表**迁移（另 3 张 LangGraph checkpoint 表由库自带迁移生成） | 必须能真实跑通 |
| M1-4 | JWT：注册 / 登录 / 刷新 / 登出，含 **refresh token 轮转 + 重放检测** | §6.3 |
| M1-5 | 多租户：Repository 层校验 + **RLS 策略（必须 `FORCE ROW LEVEL SECURITY`）** + 事务内 `SET LOCAL app.user_id` | ADR-9 |
| M1-6 | 会话级 Redis 锁（`conversation_id` 为 key，`SET NX PX`） | §7.4 |
| M1-7 | `Idempotency-Key` 中间件 + 统一错误码字典 | §7.6, 附录 C |
| M1-8 | pytest：跨租户越权用例、refresh 重放用例 | CI 必过 |
| M1-9 | 前端：登录 / 注册页（`/login`、`/register`）+ token 拦截刷新 | §12.1 |

**验收清单**

| # | 操作 | 期望结果 |
|---|------|---------|
| B1 | 空库执行 `alembic upgrade head` | 16 张业务表 + 3 张 checkpoint 表全部创建成功，无报错 |
| B2 | 重复执行 `alembic upgrade head` | 幂等，不报错 |
| B3 | 注册 → 登录 → 调用 `/api/v1/conversations` | 返回自己的空列表 |
| B4 | **用 A 用户的 token 请求 B 用户的 conversation id** | **返回 403 或空结果，绝不返回 B 的数据** |
| B5 | 用同一个 refresh token 刷新两次 | 第二次失败，且该用户**全部** refresh token 被撤销 |
| B6 | 同一 conversation 并发发两条消息 | 第二条返回 `409 CONFLICT` + `Retry-After` |
| B7 | 同一请求带相同 `Idempotency-Key` 发两次 | 第二次直接返回第一次的结果，不产生第二条消息 |
| B8 | `pytest tests/security/` | 全绿 |

**B4 是整个项目最重要的一条安全验收线。** 它验证的不是"代码写对了"，而是"租户隔离真的生效了"。这条必须有自动化测试，不能靠手动点。

**M1-3 的特别要求**：迁移脚本里包含 `vector(1024)` 与 `hnsw` 索引，所以**必须用 pgvector 镜像**，普通 `postgres:16` 镜像会在 `CREATE EXTENSION vector` 处失败。同时确认镜像内 pgvector ≥ 0.8，否则 `hnsw.iterative_scan` 不可用（M3、M6 依赖它）。

**常见坑**

- **RLS 不生效**：默认表 owner 绕过 RLS，必须 `ALTER TABLE ... FORCE ROW LEVEL SECURITY`。
- **租户 ID 泄漏到下一个请求**：用了会话级 `SET` 而非事务级 `SET LOCAL`，连接池复用时会串号。这是隐蔽且严重的问题。
- **迁移并发冲突**：不要让多个容器启动时各自 `alembic upgrade`，用独立的 migrate 步骤。

---

### M2 · 知识库入库管线（1.5 周）

**目标**：把文件变成可检索的向量与关键词索引。

**任务分解**

| 编号 | 任务 | 关键点 |
|------|------|--------|
| M2-1 | 上传接口 + 安全校验（大小 50MB / MIME + magic bytes / 配额 / 文件名净化） | §13.4 |
| M2-2 | `content_hash` 去重（同用户同文件不重复入库） | 省 embedding 成本 |
| M2-3 | 解析层：PyMuPDF（文本型 PDF / DOCX / MD / HTML） | **MVP 只用 PyMuPDF**；MinerU / 公式提取 / OCR 已裁剪（见 §9） |
| M2-4 | 去噪：按页重复度移除页眉页脚 | — |
| M2-5 | 父子分块（子 256 token / 父 1024 token / 重叠 10%）+ `chunk_type` 标记 | §6.5 |
| M2-6 | jieba 分词写入 `content_tokens`；`tsv` 为生成列 | §6.5，F13 |
| M2-7 | 千问 Embedding 批量调用（批次重试、失败记录） | 仅子块带向量 |
| M2-8 | Celery `ingest` 队列 + 断点续传 + DB 唯一约束兜底幂等 | §7.6 |

**验收清单**

| # | 操作 | 期望结果 |
|---|------|---------|
| C1 | 上传一份中文 PDF | `documents.status` 从 `processing` 变 `ready` |
| C2 | 查库 `SELECT count(*) FROM chunks WHERE document_id = ?` | 有父子两类块，子块有 embedding，父块 `embedding IS NULL` |
| C3 | 查库 `SELECT count(*) FROM chunks WHERE tsv IS NULL` | **返回 0**（tsv 生成列必须自动填充） |
| C4 | 同一文件上传两次 | 只入库一次，第二次直接被 `content_hash` 挡掉 |
| C5 | 手动把该任务重跑一次 | `chunks` 数量不变（唯一约束生效） |
| C6 | 上传一个 60MB 文件 | 返回 `UPLOAD_REJECTED` 并说明原因 |
| C7 | 上传一个改了后缀名的非 PDF 文件 | 被 magic bytes 校验拦下 |

**C3 是必须验的一条**：v1.0 的设计文档里 `tsv` 列没有任何填充机制，会静默变成空列、让关键词检索整条链路失效。这里要确认它真的在自动填充。

**常见坑**

- **中文分词**：Windows 上装 `zhparser` 需要编译 SCWS，不要走这条路；用 jieba 预处理 + `to_tsvector('simple', ...)`。
- **Embedding 维度**：第一次真实调用就确认返回 1024 维；不是的话必须在**入库前**改 DDL。
- **入库阻塞**：`ingest` 必须独立队列与独立 worker，否则一个大文件会卡住所有人的提醒任务。

---

### M3 · 单路 RAG + 可观测 + 评测基建（1.5 周）

**目标**：能基于自己的文档回答问题并给出引用；同时**建立评测基线**——这是后面所有优化的对照物。

**任务分解**

| 编号 | 任务 | 关键点 |
|------|------|--------|
| M3-1 | pgvector 单路检索：cosine + `user_id` 过滤 + `hnsw.iterative_scan` | ADR-2 |
| M3-2 | 子块召回 → `parent_id` 回溯父块 → 去重 | §8.2 |
| M3-3 | RAG 问答链路：检索 → 拼上下文 → 生成 → 引用脚注 | §8.4 |
| M3-4 | **Langfuse Cloud 接入** + trace 埋点 + **PII redaction** | §14.1 |
| M3-5 | `evals/baseline/v1_chroma_retriever.py`：迁一份 v1 单路检索实现，锁定为 baseline | F1 |
| M3-6 | `evals/golden_v1.jsonl`：先用 50 条小规模 golden set | 不追求一步到位 |
| M3-7 | `evals/noise_floor.py`：同一份代码重复跑 5 次，输出各指标 mean ± σ | §14.2.3 |
| M3-8 | 前端：知识库页面（列表/上传/进度）+ 检索调试台 | §12.1 |

**验收清单**

| # | 操作 | 期望结果 |
|---|------|---------|
| D1 | 上传 3–5 份文档后提问"XX 文档里说了什么" | 答案带 `[1][2]` 脚注，且**无脚注的事实性论断占比 < 20%**（用 groundedness 校验输出统计，不靠肉眼判断） |
| D2 | 点击引用脚注 | 能定位到来源文档与段落 |
| D3 | 打开 Langfuse，找到刚才那次对话 | 能看到完整 trace（检索 span + LLM span），含 token 与耗时 |
| D4 | 在 Langfuse 的 trace 里全文搜索测试用的手机号 | **搜不到**（PII redaction 生效） |
| D5 | 用 A 用户账号提问，语料只有 B 用户上传 | 检索不到任何 B 的内容 |
| D6 | 跑 `noise_floor.py`（5 次） | 输出 5 组分数 + mean ± σ |
| D7 | 记录本次分数 | 写入 §6 进度表，作为 baseline |

**D4 是容易被忽略但很关键的一条**：Langfuse SDK 默认会把 input/output 全量上报，脱敏必须显式配置，而且要有自动化断言——否则脱敏代码写错了你也不会发现。

**D6 的噪声地板是 M7 门禁阈值的前提。** 没有这组数字，M7 的 CI 门禁就是拍脑袋定的，会频繁假红灯然后被你自己忽略。

---

### M4 · Agent 运行时（2 周）

**目标**：从"固定链路问答"升级为真正的 Agent——会规划、会选工具、能中断恢复。

**任务分解**

| 编号 | 任务 | 关键点 |
|------|------|--------|
| M4-1 | `AgentState`（TypedDict + 显式 reducer + 预算字段） | §7.2，禁止原地修改 |
| M4-2 | `planner` 节点：产出 **DAG 计划**（`Step` + `depends_on`） | §7.1 |
| M4-3 | 并行执行器：取依赖满足的 step，`asyncio.gather`，并发上限 3 | §7.1 |
| M4-4 | ReAct 子图：四类终止条件 + 循环检测 | §7.3 |
| M4-5 | 上下文预算：工具结果截断（`max_chars` + `full_ref`）+ 滚动压缩 + 80%/100% 降级 | §7.8 |
| M4-6 | `ToolMeta` / `BaseTool` / `ToolRegistry` 三层拆分 | §7.5 |
| M4-7 | `policy.py` 本地风险策略注册表（fail-closed） | §13.6 |
| M4-8 | `mcp_servers/sandbox_server`（**重新实现**：RestrictedPython + 子进程隔离 + 资源限制。v1 的 `code_sandbox.py` 已删除，不再作为迁移来源） | §13.5 |
| M4-9 | `mcp_servers/todo_server` + `mcp_servers/search_server`（web 搜索，接第三方或自建） | ADR-4，对应 v1.1 §17 W3 的 search/todo/sandbox 三件套 |
| M4-10 | PostgresSaver checkpointer，`thread_id = conversation_id` | §7.3 |
| M4-11 | `tool_invocations` 埋点（工具失败率的数据源） | §6.8 |
| M4-12 | 集成测试：**用 fake LLM 跑通整张 Agent 图**（不真调模型，保证 CI 快且不烧额度） | §16 集成测试层 |

**验收清单**

| # | 操作 | 期望结果 |
|---|------|---------|
| E1 | 问"调研一下 XX 和 YY 的区别" | 后端日志显示 planner 产出多个 step，且**独立的 step 是并行执行的**（时间戳重叠） |
| E2 | 问"帮我加一条待办：周五交作业" | 自动调用 todo 工具，待办出现在数据库 |
| E3 | 问"计算 1234 × 5678" | 走 sandbox MCP 工具计算，结果正确 |
| E4 | 让 Agent 处理一个会返回超长内容的工具 | 日志显示结果被截断且有 `full_ref`，**上下文没有爆** |
| E5 | 连续问一个会反复调用同一工具的问题 | 在循环检测处终止，不会无限循环 |
| E6 | 查 `tool_invocations` 表 | 每条工具调用都有记录（名称/耗时/成功失败/是否截断） |
| E7 | 尝试让 Agent 调用一个未注册的工具 | 被 `policy.py` 拒绝并记审计（fail-closed） |
| E8 | 单元测试：LangGraph 状态 reducer | 并行节点写 `citations` 后不丢数据 |

**E1 是本里程碑的核心价值展示。** "深度研究类任务并行执行、耗时从 47s 降到 19s"这类简历数字就来自这里——所以 E1 执行时**要把串行与并行的耗时都记下来**。

**E8 对应一个真实的坑**：LangGraph 里节点原地修改 state 的列表元素不会触发 channel 更新。M4-1 必须一次性把 reducer 写对，否则会花大量时间排查"状态为什么没变"。

---

### M5 · HITL 审批 + 流式健壮性（1 周）

**目标**：危险动作必须人工确认；页面关了、进程挂了，任务不丢。

**任务分解**

| 编号 | 任务 | 关键点 |
|------|------|--------|
| M5-1 | 三级风险分级 + **参数级升级策略**（`args_policy`） | §7.3 |
| M5-2 | `interrupt` + `approvals` 表 + 审批状态机 | §7.3, §6.7 |
| M5-3 | **执行与传输解耦（最简版）**：`/chat` 投递任务 + `/chat/{id}/stream` 订阅（事件流不重放，重连以落库内容为准） | §7.7 |
| M5-4 | `messages` 流式落库：状态机 `streaming` / `completed` / `interrupted` / `cancelled` / `failed` + `seq` | §6.4 |
| M5-5 | 取消生成 + 重新生成（`superseded_by`） | §7.7 |
| M5-6 | 前端：审批卡片 + 停止生成（最小版） | §12.1 |

**验收清单**

| # | 操作 | 期望结果 |
|---|------|---------|
| F1 | 让 Agent 执行"发邮件"类动作 | 被拦截，前端弹出审批；批准后才执行 |
| F2 | 审批时选择"拒绝" | Agent 收到拒绝信号并给出替代回答，不卡死 |
| F3 | 发起一个长任务，中途**`kill -9` 掉 api 容器**，重启后批准该审批 | 任务从中断处**继续执行**，不从头再来 |
| F4 | 发起长任务，中途**关闭浏览器标签页**，1 分钟后重新打开 | 能看到**完整**的答案（不是空的） |
| F5 | 点"停止生成" | 立即停止；该消息标记 `cancelled`；不计入评测 |
| F6 | 点"重新生成" | 生成新消息，旧消息保留且被标记 `superseded_by` |

**F3 和 F4 是这个项目最有说服力的两个演示。** 它们证明的不是"我会用 LangGraph"，而是"我理解生产系统里进程会死、用户会关页面"。这两条建议都录屏。

---

### M6 · 生产级 RAG + 降级矩阵（1 周）

**目标**：检索质量从"能用"到"好用"，并且**任何依赖挂掉时系统都不崩、且用户知情**。

**任务分解**

| 编号 | 任务 | 关键点 |
|------|------|--------|
| M6-1 | 关键词检索路径（`tsv` + `ts_rank_cd`；**查询侧 jieba 分词**与写入侧同一分词器） | §8.2 |
| M6-2 | RRF 融合（k=60） | §8.2 |
| M6-3 | Reranker 接入（`bge-reranker-v2-m3`，**云端 API**） | §8.2 |
| M6-4 | **降级矩阵**（rerank / embedding 两条链）+ `degraded` 标记 → `messages.degraded` → SSE `degraded` 事件 → 前端角标；用 env 开关模拟依赖故障 | §8.3 |
| M6-5 | 前端：降级角标 | §12.2 |

**验收清单**

| # | 操作 | 期望结果 |
|---|------|---------|
| G1 | 在补充了真实语料题的评测集上跑两套管线（feature flag 切换） | **nDCG@10 与 Recall@10 均高于同集重跑的 M3 单路基线**（数值填入进度表） |
| G2 | 用开关模拟 reranker 故障，提问 | 答案仍能生成，且页面显示"未精排"角标 |
| G3 | 模拟 Embedding 超时，提问 | 退化为仅关键词检索，页面显示对应降级角标 |
| G4 | 记录 G1 的新分数 | 写入进度表，与 M3 基线对比（同集） |

**G2/G3 是本里程碑的灵魂。** "依赖挂了系统还能用、而且用户知道自己在看降级结果"——这是产品级思维，比任何技术名词都更能区分候选人的层次。

---

### M7 · 评测对比与 CI（0.5–1 周）

**目标**：把"M6 效果更好"变成**可复现的数字**，并建立精简 CI。

**任务分解**

| 编号 | 任务 | 关键点 |
|------|------|--------|
| M7-1 | 评测集补充 10–20 条**真实语料题**（从已上传文档出题、人工标注），并在新集上重跑 M3 单路基线 | §14.2.2 |
| M7-2 | feature flag（env / Redis 覆写）：同代码切换两套管线 | §14.3 |
| M7-3 | 版本对比表 + **逐项归因**（多少点来自混合召回、多少来自精排） | §14.2.4 |
| M7-4 | CI：GitHub Actions 跑 `ruff` + `mypy` + `pytest tests/unit`（安全集成用例依赖本地栈，本地人工跑） | §15.2 |

**验收清单**

| # | 操作 | 期望结果 |
|---|------|---------|
| H1 | 用 feature flag 一次跑出"单路 vs 混合+精排"两套管线对比表 | 对比表产出；M3 基线已在新评测集上重跑 |
| H2 | 查看对比表 | 有逐项归因，不是只有一个总分 |
| H3 | 提交一个 PR | CI 全绿（lint + mypy + 单测） |

---

### M10 · 收尾与交付资产（0.5–1 周）

**目标**：把成果做成可展示、可验证的资产。

**任务分解**

| 编号 | 任务 | 关键点 |
|------|------|--------|
| M10-1 | 三层 README + `docs/adr/` 拆分 + 架构图 | §20 |
| M10-2 | 3 分钟 demo 视频（分镜见 §20） | 演示资产 |
| M10-3 | 全新机器部署验证（照 README 走一遍） | 部署文档必须实测过 |
| M10-4 | 简历 bullet 定稿（用实测数字替换 X/Y） | §19 |

**验收清单**

| # | 操作 | 期望结果 |
|---|------|---------|
| K1 | 找一台全新机器部署 | **30 分钟内**完成部署（照着 README 走） |
| K2 | CI | 全绿（lint + mypy + 单测） |
| K3 | demo 视频 | 3 分钟内覆盖：带引用问答 → 工具调用 + 审批 → 中断恢复 → 降级角标 |
| K4 | 简历 bullet | 所有数字与实测一致，**无编造** |

---

## 6. 进度追踪表

> 每完成一项在此打勾。**没打勾的就是没做。**

### M0 端到端最小闭环
- [x] M0-1 删除 v1 内容 + 新建目录骨架
- [x] M0-2 compose.dev 编排
- [x] M0-3 FastAPI + SSE 直连 DeepSeek
- [x] M0-4 SSE 事件协议骨架
- [x] M0-5 Next.js 聊天页
- [x] M0-6 Caddy 反代（flush_interval -1）
- [x] **验收 A1–A6 全通过**
- [x] tag `m0-done`

### M1 基础设施与多租户认证
- [x] M1-1 compose 全量编排（postgres/redis）
- [x] M1-2 三角色与权限
- [x] M1-3 Alembic 16 张业务表迁移
- [x] M1-4 JWT + refresh 轮转与重放检测
- [x] M1-5 RLS + FORCE + SET LOCAL
- [x] M1-6 会话级 Redis 锁
- [x] M1-7 幂等中间件 + 错误码字典
- [x] M1-8 安全 pytest（越权/重放）
- [x] M1-9 前端登录/注册页
- [x] **验收 B1–B8 全通过**（2026-09-23，期间修复家族连坐回滚 / 幂等自撞占位 / 测试 loop 配置 3 个问题）
- [x] tag `m1-done`

### M2 知识库入库管线
- [x] M2-1 上传接口与安全校验
- [x] M2-2 content_hash 去重
- [x] M2-3 解析层（PyMuPDF）
- [x] M2-4 页眉页脚去噪
- [x] M2-5 父子分块
- [x] M2-6 jieba 分词 + tsv 生成列
- [x] M2-7 千问 Embedding 批处理
- [x] M2-8 Celery ingest 队列 + 幂等
- [x] **验收 C1–C7 全通过**（2026-09-23，C7 首轮抓到后缀/内容不一致漏洞并修复）
- [x] tag `m2-done`

### M3 单路 RAG + 可观测 + 评测基建
- [x] M3-1 pgvector 单路检索 + iterative_scan（`agent/retrieval/search.py`；GUC 在引擎连接层设置）
- [x] M3-2 父块回溯与去重（子块召回 → parent_id 归并，保留最高分子块）
- [x] M3-3 RAG 问答 + 引用（citations 事件 + `messages.citations` 落库 + groundedness 重写一次）
- [x] M3-4 Langfuse 接入 + PII redaction（双钩子 mask + 25 条自动化断言）
- [x] M3-5 baseline 实现（v1 Chroma 单路，`evals/baseline/v1_chroma_retriever.py`）
- [x] M3-6 golden 50 条（`evals/golden_v1.jsonl` + 10 篇锚点语料 `data/evaldocs/`）
- [x] M3-7 噪声地板实验（`evals/reports/noise_floor_m3.json`，5 次重复）
- [x] M3-8 知识库页面 + 检索调试台（`/kb`、`/kb/debug`、聊天页引用脚注）
- [x] **验收 D1–D7 全通过**（记录见 `M3_验收手册.md`）
- [x] **记录 baseline 分数**：v2 `Recall@10=1.000 / MRR@10=1.000 / nDCG@10=0.9978`，检索段 P50 160ms / P95 228ms（< 300ms 预算）；v1 Chroma baseline 同尺 `1.000 / 1.000 / 0.9978`——**合成语料区分度不足，两版均饱和，M7 扩集须混入真实语料**（详见手册「M3 baseline 记录」）
- [x] tag `m3-done`

### M4 Agent 运行时
- [x] M4-1 AgentState + reducer（merge_citations/chunks 去重合并；E8 含真实 LangGraph 并行图端到端）
- [x] M4-2 planner（DAG）（≤6 步 + 依赖校验 + 解析失败回退 chat + 降级标记）
- [x] M4-3 并行执行器（依赖就绪 gather 上限 3；重试 2 次；失败跳过传递依赖；重规划 1 次）
- [x] M4-4 ReAct 循环 + 终止条件（无调用/8 轮/预算阈值/同参数重复 → 循环检测）
- [x] M4-5 上下文预算管理（registry 截断+full_ref 唯一截断点；滚动压缩；80%/100% 降级）
- [x] M4-6 ToolMeta/BaseTool/ToolRegistry（超时/幂等/限流/截断/埋点/审计零成本继承）
- [x] M4-7 本地风险策略注册表（fail-closed：未注册拒绝+审计；参数级 deny/升级；L2 显式拦截待 M5）
- [x] M4-8 sandbox MCP server（RestrictedPython + python -I 孙进程 + rlimit CPU3s/内存256MB/禁fork + 墙钟杀）
- [x] M4-9 todo + search MCP server（todo 走 worker DSN，dedupe_key 治理层注入；search Tavily 可配/DDG 兜底）
- [x] M4-10 PostgresSaver checkpointer（AsyncPostgresSaver，thread_id=conversation_id，worker DSN）
- [x] M4-11 tool_invocations 埋点（成功/失败/去重/截断全路径，唯一收敛于 registry.finally）
- [x] M4-12 集成测试（fake LLM 全图 E1–E7，7 passed，不烧额度）
- [x] **验收 E1–E8 全通过**（记录见 `M4_验收手册.md`）
- [x] **记录串行 vs 并行耗时**：集成测试（两个 0.8s 慢工具）并行 wall<1.6s vs 串行≥1.6s；真实端点 E1 两调研步 `started_at` 相同（43872.26）、区间重叠
- [x] tag `m4-done`

### M5 HITL 审批 + 流式健壮性
- [ ] M5-1 风险分级 + 参数级策略
- [ ] M5-2 interrupt + approvals 状态机
- [ ] M5-3 执行与传输解耦（最简版）
- [ ] M5-4 消息流式落库 + seq
- [ ] M5-5 取消 + 重新生成
- [ ] M5-6 前端审批卡片
- [ ] **验收 F1–F6 全通过**
- [ ] tag `m5-done`

### M6 生产级 RAG + 降级矩阵
- [ ] M6-1 关键词检索路径（含查询侧 jieba）
- [ ] M6-2 RRF 融合
- [ ] M6-3 Reranker 接入
- [ ] M6-4 降级矩阵（rerank/embedding）+ degraded 事件
- [ ] M6-5 前端降级角标
- [ ] **验收 G1–G4 全通过**
- [ ] **记录 M6 分数（同集对比）：__________**
- [ ] tag `m6-done`

### M7 评测对比与 CI
- [ ] M7-1 评测集补真实语料题 + 重跑 M3 基线
- [ ] M7-2 feature flag
- [ ] M7-3 对比表 + 逐项归因
- [ ] M7-4 GitHub Actions（lint + mypy + 单测）
- [ ] **验收 H1–H3 全通过**
- [ ] tag `m7-done`

### M10 收尾与交付资产
- [ ] M10-1 README + ADR 拆分 + 架构图
- [ ] M10-2 demo 视频
- [ ] M10-3 全新机器部署验证
- [ ] M10-4 简历定稿
- [ ] **验收 K1–K4 全通过**
- [ ] tag `v2.0`

---

## 7. 每个里程碑的通用检查动作

除各里程碑自己的验收清单外，每个里程碑收尾时都要跑一遍这五项：

| 检查 | 命令/方式 | 通过标准 |
|------|----------|---------|
| 代码质量 | `ruff check .` + `mypy .` | 无 error |
| 单元测试 | `pytest tests/unit` | 全绿，且**覆盖率 ≥ 80%**（对应 §16 单元测试层要求） |
| 集成测试 | `pytest tests/integration` | 全绿（M4 起：fake LLM 图） |
| 迁移可跑 | 空库执行 `alembic upgrade head` | 无报错（F45 的教训：DDL 必须真实执行过） |
| 租户隔离 | `pytest tests/security` | 全绿 |

**"迁移可跑"这一项从 M1 起每个里程碑都要跑**，因为后续里程碑会不断加表加列，任何一次写错都会在部署时才暴露。

---

## 8. 风险与应对

| 风险 | 触发信号 | 应对 |
|------|---------|------|
| 环境问题与技术问题混淆 | M0 卡超过 5 天 | 回到 §2.4 的三个独立验证，逐个排除 |
| 范围膨胀 | 想加设计文档 §1.3 非目标里的东西 | 记入 backlog，不插队；每周五复盘 |
| 评测集造假或注水 | 分数好得不像话 | 评测集必须固定、可复现；M7-1 强制引入真实语料题并在新集上重跑基线 |
| LLM 成本失控 | 月度账单超预算 | 日配额 + 成本归因（§5.2）；先跑小规模 |
| 排期拖延 | 里程碑连续两次超期 | 优先保住可演示的主干闭环（审批 / 中断恢复 / 降级），砍非核心完善项 |
| 编码能力被 AI 代劳 | 讲不清某段代码为什么这么写 | 每个里程碑结束写一段"本次涉及的原理"笔记（见 `学习难点笔记.md`） |
| 单点故障（只有你懂） | — | 每个里程碑更新 README 与 ADR，降低"三个月后自己看不懂"的风险 |

**最后一条值得单独强调**：这个项目要用来面试，**你必须能讲清每一处设计的原因**。所以每个里程碑结束时，建议花半小时把"这一步为什么这么做、踩了什么坑"记进 `学习难点笔记.md`——面试前那份笔记就是你的复习材料。

---

## 9. 与设计文档的差异记录

两份文档已按最终范围同步（设计文档为 v1.2）：

| # | 差异 | 原因 | 影响 |
|---|------|------|------|
| 1 | **补齐代码目录结构**（§3.2） | v1.1 用"非功能性需求"章替换了 v1.0 的代码结构章，新结构缺权威定义 | 无风险，纯补充 |
| 2 | **全程云端模型**（DeepSeek + 千问 + 云端 Reranker），**不部署本地模型** | 本地小模型会掩盖检索层的问题、破坏评测归因；与"应用工程"定位无关 | 设计文档 ADR-8 已同步改写为"统一走云端" |
| 3 | **排期约 10.5 周**（M0–M7 + M10 收尾） | 把"环境准备 + 验收 + 排障"显式计入 | 面试时估时能力本身被考察 |
| 4 | **M0 新增"端到端最小闭环"** | v1.1 从基础设施开始，环境风险后置 | 提前暴露环境问题 |
| 5 | **M2 解析层只用 PyMuPDF** | 避免 M2 被解析质量拖住 | MinerU / 公式提取 / OCR 列为 v2 候选 |
| 6 | **评测分两阶段**（M3：50 条 golden set + 噪声地板；M7：补真实语料题 + 对比表与归因） | 一步到位构造大评测集会阻塞 M3 | 评测基建提前，对比归因后置 |
| 7 | **baseline 明确为"v1 Chroma 单路检索的最小可复现实现"** | v1 无评测脚本，baseline 需新建而非迁移 | 见评审报告 F1 |
| 8 | **v1 内容全部删除，不归档**（2026-09-22 已执行） | 保持仓库干净，只保留 v2 需要的文件 | v1 仍存于提交 `dae6a9ee` 中，可恢复 |
| 9 | **范围收敛（2026-09-23）**：实施范围为 M0–M7 + M10 收尾，聚焦主干闭环与演示资产 | 目标函数是"面试展示价值最大化"而非功能完整 | 设计文档已同步收敛为 v1.2（删除未纳入范围的章节）；未纳入范围的表结构随 M1 迁移保留在 DDL 中 |

**至此本节无待确认项。**

---

## 10. 下一步

**当前状态（2026-09-23）**：M0–M3 已交付并验收（tag `m0-done` ~ `m3-done`）；M4 Agent 运行时进行中（代码已就位，待修完集成用例并通过 E1–E8 验收）。

**推进方式**：按 §4.3 的统一流程逐里程碑推进——交付代码 + 验收手册 → 本地验收 → 打勾进度表 + 打 tag。
